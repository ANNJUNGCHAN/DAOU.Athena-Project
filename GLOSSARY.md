# Athena 용어집

이 저장소에서 쓰는 말의 **표준 정의**. 문서·코드·커밋·대화 전부 여기 표기를 따른다.
새 용어를 만들면 여기에 먼저 등록한다. 등록 없이 쓰는 말은 없다.

> 표기 규칙: **상태**는 `구현됨` / `설계 확정, 미구현` / `미착수` / `보류` 중 하나.
> 근거는 실제 파일·줄번호. 근거 없는 항목은 넣지 않는다.

---

## 1. 창 · 표면 ★

Athena의 UI 표면은 **창 2개**다. 그 외의 모든 화면은 **대화 창의 모드**이지 창이 아니다.
"창"이라는 말을 쓸 때 창인지 모드인지 반드시 구분한다.

### 창 (window) — OS 레벨로 분리된 독립 창. 둘뿐이다

| 용어 | 정의 | 코드 | 상태 |
|---|---|---|---|
| **대화 창** (= 채팅창) | **유일한 입력 지점.** 자연어 입력 + 최소 면적의 이력. 리퀴드 글래스 표면. 위로만 자라고 입력줄이 있는 하단 모서리는 고정된다 | `chatWin` — `app/main.js:99` | 구현됨 |
| **캔버스 창** | **유일한 출력 지점.** **카드**가 뜨고 조합되고 접히고 펼쳐진다. 부팅 시엔 숨겨져 있다가 점→확장으로 드러난다 | `canvasWin` — `app/main.js` | 구현됨 |

> **⚠ 캔버스 창도 유리다. "불투명"이라 적힌 문서는 낡았다.**
> `ui/soul.md` §7 재료 규범: *"모든 표면이 유리다 — 두 창도, 창 안의 카드도 전부."*
> `ui/liquid-glass.md:99`가 "캔버스는 불투명" 결론을 **2026-08-14에 폐기**했다.
> 실측: `app/main.js`의 `commonWinOpts`가 **두 창 모두**에 `backgroundMaterial: 'acrylic'`을 준다 —
> 캔버스 창은 OS 레벨에서 이미 유리다. 차이는 **유리 두께**뿐이다. §7 규범상 데이터 위 유리는
> 두꺼워야 한다(~0.97). 얇으면 뒤가 비쳐 숫자가 겹쳐 읽히고 그건 §8 "정보 정직성" 위반이다.

두 창은 한 창의 두 영역이 아니라 **OS 레벨에서 분리된 별개의 창**이다. 각자 따로 움직이고, 따로
신축하고, 따로 닫힌다 (`ui/soul.md:40-41`). 겹치면 유리가 **캔버스**를 굴절시키고, 나란히 두면
유리가 **데스크톱**을 굴절시킨다.

### 대화 창의 모드 — 창이 아니다 (2026-08-16 확정)

대화 창은 하나지만 여러 **모드**를 갖는다. 어느 모드도 새 창을 만들지 않는다.

| 용어 | 정의 | 진입 경로 | 상태 |
|---|---|---|---|
| **대화 모드** | 기본. 입력 + 최소 면적의 이력 | — | 구현됨 |
| **온보딩 모드** | CLI 연결 · 계좌 연결 | 최초 실행 시 1회 | **구현됨** — `app/chat.js` `startOnboarding`, `app/lib/onboarding.js`. 대화 창의 `#onboard` 패널(`app/chat.html:37`) |
| **인증 모드** | 토큰 상태 · 토큰 4상태 · 계좌 전환 | 계좌 등록 직후 1회, 이후 재진입 | **구현됨** — `app/chat.js` `showAuthConfirm`, `app/lib/auth-screen.js`. 같은 `#onboard` 패널 |
| **설정 모드** | 계좌 · MCP 카드. **이력 보관 정책 · 관측 주기 · 접근성 3종은 미착수** | ① **입력 영역의 점(`#dot`)을 누른다** ② 커맨드바(`SETTINGS_COMMAND` — "설정", "계좌 관리", "모델 바꿔줘") ③ Esc로 대화로 돌아온다 | **구현됨** — `app/chat.js` `openSettings`, `app/lib/settings-cards.js` → 대화 창의 `#settings` 패널(`app/chat.html`) |
| **주문 확인 모드** | **주식 주문의 최종 집행 지점.** 주문은 Athena가 자동 집행하지 않으므로(확정 결정 3), 사람이 최종 집행하는 지점이 여기다 | 주문 플로우 | 미착수 |

- **모드는 창 개수에 세지 않는다.** 창은 둘이고 예외가 없다 — `ui/soul.md` §3, §8 탈락 조건
  "창이 셋 이상이다".
- 근거: **설정을 만지거나 주문을 확인하는 동안 사용자는 채팅을 치지 않는다.** 두 기능이 시간을
  다투지 않으므로 면적을 나눌 이유가 없다.
- **이력 열람은 설정 모드가 아니라 대화 모드의 일이다.** 값을 정하는 곳과 내용을 보는 곳을 섞지 않는다.
- **설정은 커맨드바로도 반드시 도달할 수 있어야 한다.** 점은 *추가* 진입로다. 커맨드바가
  아닌 경로로**만** 갈 수 있으면 §8 탈락 조건에 걸린다.
- **창이 둘이라는 것은 실측이다** (2026-08-16, 브랜치 4종 병합 후). `app/main.js`의
  `BrowserWindow` 생성은 `warmup`(10×10 임시 창, 즉시 close) · `canvasWin` · `chatWin`
  셋뿐이고 `dialog.*` / `showMessageBox` 호출은 없다. 미착수인 모드는 **주문 확인** 하나다.
- **모든 모드는 대화 창 안에 산다.** 이름 그대로다. 각 모드는 `chat.html`의 형제 패널이고,
  열릴 때 `#app`(대화 화면)이 물러난다. 창은 만들어지지 않는다.
  - 온보딩·인증 → `#onboard` 패널 (`app/lib/onboarding.js`, `auth-screen.js`)
  - 설정 → `#settings` 패널 (`app/lib/settings-cards.js`)
  - 셋 다 같은 문법이다 — 대화 창이 `chatMaxH`로 자라고, 끝나면 `chatBaseH`로 돌아온다.
- **⚠ 2026-08-16 이전 코드는 설정 카드를 캔버스 창에 그렸다.** `canvas.js`가
  `renderAccounts`/`renderMcp`를 import했다. 그건 "대화 창의 모드"라는 결정과 어긋나서 옮겼다.
  옛 커밋·캡처를 볼 때 이 차이를 감안하라.

> **폐기된 용어: "일시 표면" · "설정창" · "팝업창".** 2026-08-16 이전 문서는 설정과 주문을
> 별도 창(일시 표면)으로 기술했다. **창은 둘이라는 원칙을 지키기 위해 전부 대화 창의 모드로 접었다.**

### ⚠ 타이틀바는 없다 — 실측

`app/main.js:60`은 두 창 모두 `frame: false`이고 `app/chat.html`에 타이틀바 마크업이 0줄이다.
`ui/round-1R/two-windows.md:122`는 *"캔버스 창의 타이틀바가 필요한가"*를 **미해결로 남겨두고 있다.**
화면설계서가 한때 `● ● / 대화 / CLAUDE` 타이틀바를 그렸으나 **2026-08-16에 실측을 따라 제거했다**
(`CLAUDE.md` §3 "실측이 문서를 이긴다"). 창 제어는 그립(`#grip`)과 OS 단축키로 한다.

### 관련 용어

| 용어 | 정의 | 근거 |
|---|---|---|
| **커맨드바** | **대화 창과 같은 말이다.** 별도 요소가 아니다 — 대화 창 자체가 커맨드바다. 설정·모델·주기·계좌 연결의 **정규 진입로**. "설정 아이콘을 찾아 헤매는 경험은 Athena에 없다" | `ui/soul.md:16,52` |
| **인터랙티브 창** | 캔버스 창의 다른 표현. 자유자재로 줄었다 늘었다 하는 창 | `ui/soul.md:88` |
| **점 (`#dot`)** | 입력 영역 좌측의 12px 마젠타 원. ① 대화 상태 표시(`judging` breathe / `calling` pulse) ② 캔버스 확장 애니메이션의 중심점 ③ **설정 모드 진입** | `app/chat.html:24`, `app/chat.css:132-150`, `app/main.js:137-180` |
| **워밍업 창** (`warmup`) | topmost 합성 버그 우회용 10×10 임시 창. 즉시 닫힌다. **UI가 아니다** | `app/main.js:82` |
| **E3 / E3-확장** | 채택된 두 창 배치안(상하 나란히). E3-확장은 대화 창이 위로 자라 캔버스 창 위로 올라탄 상태 | `ui/round-1R/two-windows.md:5,36,54,73` |

---

## 2. 카드

> **용어 확정 (2026-08-17).** **"캔버스"는 캔버스 창을 뜻한다. 그 안에 들어가는 것은 "카드"다.**
> 이전 문서는 안에 들어가는 것도 "캔버스"라 불러서 창과 내용물이 같은 이름을 썼다. 그 용법은 폐기됐다.

### 카드란 무엇인가

**카드는 데이터를 표현하는 공통 UI다.** Power BI나 Tableau의 시각화 컴포넌트와 같은 위치다 —
**들어오는 데이터가 달라진다고 UI가 달라지지 않는다.** 키움 TR이 몇 개든, 응답 스키마가 무엇이든,
카드의 종류는 고정돼 있고 데이터가 그 안에 들어갈 뿐이다.

이게 핵심이다. 카드는 *화면*이 아니라 **데이터 형상에 대응하는 표현 문법**이다.
TR 하나에 화면 하나를 그리면 288개를 그려야 한다. 카드는 그걸 **열두 종으로 접는다.**

> **따라서 "TR이 늘었으니 카드를 하나 더 만든다"는 언제나 틀린 판단이다.**
> 새 카드가 필요해 보이면 먼저 기존 12종 중 어디에 접히는지 본다. 정말로 안 접히면 자유 카드다.

| 용어 | 정의 | 상태 |
|---|---|---|
| **카드** | 캔버스 창 안에 뜨는 데이터 표현 단위. **12종 고정.** 데이터가 바뀌어도 카드 UI는 안 바뀐다 | 3종 구현됨 (`app/canvas.js` `addCard`) |
| **모자이크** | 캔버스 창 안에서 카드들이 조합되는 배치. 원형 `clip-path`로 확대/축소하며 드러난다 (`#mosaic`) | 구현됨 (`app/canvas.js`) |
| **자유 카드** | 기성 12종으로 표현 못 하는 데이터가 오면 AI가 그 자리에서 그리는 **설계된 탈출구**. 자주 열리면 카드 체계가 틀린 것 | 개념 확정, 미착수(W4) |
| **카드 칩** | 답변 완료 후 어느 카드를 만들었는지 잇는 UI 요소. 클릭하면 해당 카드가 강조된다 | 구현됨 (`app/chat.js`) |

### 카드 12종 (`plan/canvas-taxonomy.md`) — 초안, 미구현

가격 시계열 · 다축 오버레이 시계열 · 랭킹 테이블 · 호가창 · 종목 스냅샷 · 체결/이벤트 로그 ·
계좌·포트폴리오 · 주문 티켓·블로터 · 분포 · 업종/테마 히트맵 · 조건검색 빌더 · ELW 지표 패널
(+ 자유 카드)

### 신규 카드 4종 (`plan/mcp-실행계획.md`) — 설계 확정

| 용어 | 정의 |
|---|---|
| **스트림** (신규①) | 시간순 레코드(뉴스·공시 통합). 필드 `ts/source/title/url` |
| **리더** (신규②) | 장문 문서 1건. TOC는 클라이언트가 유도, AI 요약이 근거를 하이라이트 |
| **타임라인** (신규③) | 가격축 × 이벤트 핀. MCP 없이는 불가능한 유일한 카드 |
| **공통 테이블** (신규④) | 스키마 불특정 레코드. **"부모"이자 기본값** — 랭킹 테이블이 그 자식. 원장·규칙·감시가 전부 여기 산다 |

> ⚠ **"공통 캔버스"는 두 뜻으로 쓰인다.** §12 참조.

---

## 3. 리퀴드 글래스 — 재료 어휘

| 용어 | 정의 | 근거 |
|---|---|---|
| **리퀴드 글래스** | 블러가 아니라 빛을 굴절·집중시키는 "dynamic, digital meta-material". Athena의 핵심 재료 | `ui/liquid-glass.md:14` |
| **굴절** | 유리 뒤 콘텐츠가 유리를 통해 비치는 광학 현상. **유리의 존재 근거** — 뒤에 굴절할 게 없으면 그건 반투명 회색 사각형이지 유리가 아니다 | `ui/soul.md:125,151` |
| **유리 두께** | 고정값이 아니라 **뒤에 있는 것의 정보 밀도와 밝기에 비례**한다. 어두운 데스크톱 위 ~0.82 / 데이터 캔버스 위 ~0.97 / 밝은 배경 위 ~0.94↑ | `ui/soul.md:127` |
| **광량 3단계** | 데스크톱 → 창 → 캔버스로 **고정된** 밝기 위계. 같은 단계끼리는 값이 같아야 한다 | `ui/soul.md:146` |
| **Regular 변형 / Clear 변형** | Apple 리퀴드 글래스의 두 변형. Athena는 **Regular 하나만** 쓰고 Clear와 섞지 않는다 | `ui/liquid-glass.md:37-43` |
| **프로스트 (서리유리)** | 유리 뒤 콘텐츠가 선명히 비치지 않고 흐려진 상태. **정적 프로스트**는 실시간 캔버스의 굴절 재계산을 피하려고 미리 구운 유리 | `ui/soul.md:144,147` |
| **sheen** | 표면 반사. 배경이 밝을수록 강해지고, 로고 화살표와 같은 **45°**로 흐른다 | `ui/palette.md:186,192` |
| **Lensing / Light play / Transparent yet defined / Graceful transitions** | Apple이 정의한 리퀴드 글래스 4대 거동. **Graceful transitions** = 등장·소멸을 페이드가 아니라 **굴절 변조**로 처리 | `ui/liquid-glass.md:20-23` |
| **흡수 / 산란 / 반사 / 각인** | 유리 표현의 4가지 물리 근거. 각인은 텍스트가 유리 *위*가 아니라 *안*에 있어 보이게 하는 `text-shadow` | `ui/palette.md:190-193` |
| **다크 스톱** | 유리 뒤 표면색이 배경색이 아니라 유리의 색 정지점으로 들어간다는 개념 | `ui/palette.md:24` |
| **45°** | 로고 화살표 각도에서 뽑아 시스템 전체에 고정한 사선 각도(sheen, 굴절 스윕, 로딩 게이지, 캔버스 등장 방향) | `ui/brand.md:16,22-29` |

---

## 4. 색 · 타이포 토큰

| 토큰 | 값 / 의미 |
|---|---|
| `--color-k-bg` / `--color-k-panel` / `--color-k-panel2` / `--color-k-panel3` | `#10131a` 데스크톱 / `#171b24` 창 / `#1d222c` 캔버스 / `#242a36` 캔버스 내부 융기 |
| `--color-k-text` / `--color-k-dim` / `--color-k-faint` | 본문 / 보조 / 최약 텍스트 |
| `--color-up` / `--color-down` / `--color-flat` | `#ff5c5c` 상승 / `#4d9fff` 하락 / `#98a0b0` 보합 — 국내 관례 |
| `--color-brand` | `#ee137b` 키움 마젠타. **유일한 액센트** |
| `--color-bad` | `--color-up`과 값이 겹쳐 **Athena에서 사용 금지** |
| `--glass-alpha` | 유리 두께의 실제 조절값(0.82↔0.97). Electron `backgroundMaterial`은 런타임 블러 반경을 안 열어주므로 **실제로 조절 가능한 건 이것뿐** |
| `--font-mono` | Geist Mono. 정렬이 생명인 숫자(호가 사다리, 랭킹 테이블, 타임스탬프, TR 코드) 전용 |
| **다키체** (Daki / Daki B / Daki Title) | 채택 한글 서체 3패밀리. 전부 weight 400 고정 — 굵기는 **패밀리명 교체로만** 바꾼다 |

**색은 정보다.** 채도 높은 색은 "지금 이걸 봐야 한다"는 뜻이다. 화면에 유채색 요소가 셋을 넘으면
그 화면은 실패다 (`ui/soul.md:69-72`).

---

## 5. 판정 · 프로세스

| 용어 | 정의 |
|---|---|
| **판정 축** | 시안을 채점하는 7개 기준 — 철학 부합 / 미니멀 강도 / 캔버스 확장성 / **정보 정직성** / **물성** / **AI 냄새 없음** / 첫인상. 각 5점 |
| **정보 정직성** | 금융 데이터를 왜곡·오독 없이 보여주는가. **숫자가 읽히는가.** 미감보다 위다 |
| **물성** | 유리가 진짜 유리 같은가, 깊이가 가짜가 아닌가 |
| **AI 냄새** | 원샷 프롬프트 화면들의 공통 특징 목록. 보라→분홍 그라디언트, 균등 3카드, 이모지 아이콘, 무의미한 글로우, **장식용 유리**, **페이드로 등장하는 유리**, 대칭 강박, 없는 기능을 암시하는 장식 UI |
| **탈락 조건** | 점수와 무관하게 즉시 제외 — **상시 창 셋 이상** / 상시 사이드바·탭바·툴바 / 설정이 커맨드바 아닌 경로로**만** 접근 / 금지 목록 2개 이상 위반 |
| **라운드 / 시안 / 델타** | YC 프로세스 단위. 라운드마다 시안 N개를 만들고 랭킹한 뒤 **델타**(차이·개선점)를 다음 라운드 프롬프트에 주입. 6라운드 반복 |
| **무드보드 / 핀 / DNA** | Pinterest 선별 이미지 모음 / 개별 이미지(8장) / 핀들의 공통 특징 |

**핵심은 생성이 아니라 선별이다.** 라운드가 돌수록 좋아지는 게 아니라 후진 게 하나씩 사라진다.

### 대화 3상태

| | 문서 표기 | 코드 값 | UI 문구 |
|---|---|---|---|
| 1 | 판단 중 | `judging` | "어떤 TR을 부를지 고르는 중" |
| 2 | 호출·렌더 중 | `calling` | "{tool} 불러오는 중" → "{tool} 완료" |
| 3 | 완료 | *별도 상태 없음 — `idle`로 수렴* | 답변 렌더 + 카드 칩 + 트레이스 |

> 문서는 3상태라고 쓰지만 **코드의 상태값은 `idle`/`judging`/`calling` 셋**이고 "완료"는 별도
> 상태가 아니다 (`app/chat.js:17,145-227`). 문서를 고칠지 코드를 고칠지 아직 안 정했다.

| 용어 | 정의 |
|---|---|
| **점 (dot)** | 대화 창의 `#dot`. 상태 표시 · 캔버스 확장 중심점 · **설정 모드 진입** 세 역할을 겸한다 — 전체 정의는 §1 |
| **그립 (grip)** | 대화 창 하단의 드래그 핸들. 위로 끌면 창이 커진다 |
| **게이지 (gauge)** | 부팅 진행 바(`#gaugeFill`, 1.3초 후 페이드아웃) |
| **트레이스** | 답변 메타에 붙는 호출 TR 이름 + 소요 시간 (예: `search_news · reader · table · 2.05s`) |

---

## 6. 키움 · 백엔드

| 용어 | 정의 |
|---|---|
| **TR / TR ID** | 키움 REST API의 거래요청 단위 식별자 (`ka10001`, `kt00018`). 원본 208개. 사용자에게는 한국어로 매핑해 보여주되 근거는 감추지 않는다 |
| **base operation** | `group_id`가 없는 오퍼레이션. 업스트림에 실제로 나가는 단일 요청이며 **항상 완전한 응답** |
| **detail projection** | `group_id`가 있는 파생 route. base와 **똑같은 업스트림 호출**을 하고 응답만 필드 별칭으로 좁힌 것 — 더 싼 호출이 아니다. 115개 |
| **오퍼레이션 아이덴티티** | transport 세부를 UI에 노출하지 않기 위한 식별 필드. 대소문자 구분, 이름 변경은 파괴적 계약 변경 |
| **공통화면** | 갈래 B 용어 — 키움 264개 read/display 매핑을 렌더하는 shared canvas shell |
| **ScreenDefinition** | routable mapping마다 정확히 하나 존재하는 생성·버전·불변 표현 정의 (screenId/source/mode/controls/sections/continuation/safety/provenance). 설계 확정, 미구현 |
| **ScreenDocument** | 런타임 결과 문서. state(loading/ready/empty/error/unavailable/auth_required/action_required) + section(facts/table/event_stream/status/action/order_receipt). 설계 확정, 미구현 |
| **초당 5건 (호출 예산)** | 롤링 1초 최대 5건, 같은 API ID는 1건. REST·WebSocket 제어·주문이 **하나의 리미터**를 공유한다. UI 설계 제약이기도 하다 |
| **fail-closed** | 자격증명이 없으면 데이터 라우트가 503을 낸다. 조용한 빈 결과는 없다 |

---

## 7. LLM 셀렉터

전체 스키마를 모델 컨텍스트에 넣지 않고 `search → describe → resolve → call`로 좁히는 컨트롤 플레인.
**노출 툴은 정확히 4개.**

| 용어 | 정의 |
|---|---|
| `athena_search` | 질문 문자열로 카탈로그를 검색해 후보 오퍼레이션을 랭킹 순으로 반환 |
| `athena_describe` | `operation_ref` 하나의 정확한 요청/응답 필드 계약을 반환 |
| `athena_resolve` | 질문+인자를 검증해 오퍼레이션을 확정하고 **서명된 단기 실행 계획**을 발급 |
| `athena_call` | `athena_resolve`가 발급한 서명 plan에 **봉인된** 오퍼레이션/인자만 실행. 바꿔치기 불가 |
| `llm_get_manifest` | 부트스트랩용 GET 엔드포인트. **다섯 번째 툴이 아니다** (`x-athena-llm-exposed: false`) |
| **실행 계획 (plan token)** | `operation_ref` + 검증된 인자 + 만료시각을 HMAC-SHA256으로 서명한 `v1.<payload>.<sig>`. TTL 1~600초(기본 120). **URL·경로를 담지 않는다** |
| **family (TR 패밀리)** | `base:{tr_id}`. 하나의 TR과 그 모든 detail projection이 공유하는 검색·선택 단위 |
| **policy** | 질문에서 **패밀리 하나만** 고르고 projection은 **절대 추측하지 않는다**는 원칙. 확신도 240점, 1·2위 격차 <80점 또는 비율 <1.15면 애매로 판정 |
| **catalog** | 323개 오퍼레이션을 `operation_ref`로 색인한 불변 카탈로그. 버전은 `sha256:...` |
| **lexicon** | 버전 관리된 한/영 금융 동의어 사전 (`ko-en-finance-v1`) |
| **ranking** | 정체성 매치 → 제목·도메인·필드 매치 → 동의어 → 커버리지 보너스를 점수화하는 **결정론적** 정렬. 임베딩·모델 호출·무작위성 금지 |
| **visibility** | `normal` 정상 노출 / `explicit` discovery-only(주문·WS는 검색은 되나 호출 금지) / `hidden` 없는 것처럼 취급(oauth) |
| **generic_callable** | 일반 셀렉터로 호출 가능한가. query kind만 참. 286/323 |

---

## 8. MCP 게이트웨이

| 용어 | 정의 |
|---|---|
| **MCP 게이트웨이** | Athena가 등록된 외부 MCP 서버 N개에 **클라이언트**로 붙고, 집계한 툴을 단일 MCP **서버**로 CLI에 재노출하는 구조 |
| **upstream 서버** | 사용자가 등록한 외부 MCP 서버 `{command, args, env}`. `~/.athena/mcp_servers.json`에 영속 |
| **별칭 (alias)** | 사용자가 서버에 직접 부여하는 이름. **네임스페이스 키는 반드시 별칭** — `serverInfo.name`은 서로 다른 서버 셋이 같은 이름을 보고한 실측이 있다 |
| **`별칭__툴명`** | CLI에 노출하는 합성 이름. 64자 상한 위반 시 조용히 자르지 않고 **스킵 + 사유 기록**. 별칭 28자 상한은 여기서 역산된 값 |
| **리졸루션 테이블** | `별칭__툴명 → (별칭, upstream 원본명)` 매핑. 별칭이 바뀌어도 이미 발급된 이름은 계속 풀린다 — **in-flight 호출이 안 깨진다** |
| **동의 게이트** | 서버 등록과 승인은 **별개 사건**이며, spawn 직전 `require_server_approved()`를 통과해야만 프로세스가 뜬다는 보안 계층. 우회 금지 |
| **quirks** | 실측으로 확정된 upstream 결함만 보정 — `corp_code` 숫자→8자리 문자열, 모지바케(U+FFFD) **탐지만**(자동 복구 안 함), `truncate_at` 상향 제안. **추측 금지** |
| **4단계 결과 파싱** | `isError` → `structuredContent` → `content[0].text` + `json.loads` → 순수 텍스트. **실제 주경로는 3번**(서버 3개 중 2개가 `structuredContent`를 항상 `null`로 준다). 순서 재배열 금지 |
| **진행토큰 리맵** | 다운스트림 진행토큰을 upstream 세션 토큰에 매핑해 취소 알림을 올바른 세션으로 전달 |

---

## 9. 투자 브레인

| 용어 | 정의 |
|---|---|
| **투자 브레인** | FastAPI가 단독 소유하는 embedded LadybugDB 그래프 투영. 검색은 **그래프 순회 + BM25만** 쓴다 |
| **히스토리** | 채팅/체결 완료 거래를 SQLite(WAL)에 원본 그대로 담는 **권위 있는** 원시 이력 |
| **투영 (projection)** | 히스토리 변경분을 그래프로 옮겨 적재한 **파생** 데이터. 항상 히스토리에서 재구축 가능해야 한다 |
| **인제스천** | 어댑터가 원시 이력 변경분을 폴링→큐잉→잡 실행(지수 백오프 재시도)하는 파이프라인 |
| **노드 타입** | `Entity`(investor_profile/security/company/sector/theme/goal/preference/risk_signal), `SourceRecord`(chat_message/conversation/trade/research), `Claim`(observation/inferred_preference) |
| **엣지 타입** | `RELATES_TO`(relates_to/interested_in/prefers/owns/traded/researched/belongs_to/exposed_to/avoids/targets), `ABOUT`(Claim→Entity), `SUPPORTED_BY`(Claim→SourceRecord) |
| **LadybugDB** | 임베디드 그래프 DB. **단일 소유자 스레드**로 접근을 직렬화한다 |

**상태:** 골격 동작, **FastAPI 결선은 미완** — 브레인용 라우트 표면이 아직 없다.

---

## 10. 감시 에이전트 (전부 미착수 — 설계만)

| 용어 | 정의 |
|---|---|
| **조용한 감시자** | 사용자가 안 볼 때도 보유 종목·뉴스·차트를 훑어 이상 징후를 찾는 기능. **중요한 것만 알린다** |
| **파수꾼** | **LLM을 쓰지 않고** 결정론적 임계값으로 시세·뉴스를 상시 감시하는 얕은 계층 |
| **조사관** | 파수꾼이 깨울 때만 도는 LLM 기반 상관 분석기. 인과를 단정하지 않고 정량 게이트(선행성·고유성·유의성)를 통과한 후보만 다룬다 |
| **원장 (Ledger)** | **발화 / 근접 / 억제** 판정을 기록하는 감사 로그. "왜 **안** 불렀나"에 답하기 위해 존재한다. 공통 테이블 캔버스의 인스턴스 |
| **관측 임계 vs 알림 임계** | 파수꾼은 **낮은** 임계로 상시 관측·기록하고, 알림은 **높은** 임계만 발동. 보수적으로 시작하면 원장이 비는 문제를 푼다 |
| **트리거 6종** | 급등락 / 거래량 급증 / 호가 불균형 / VI 발동 / 체결강도 급변 / 52주 신고·신저 |
| **규칙 컴파일러** | 자연어 규칙을 **컴파일 시점 1회만** LLM으로 해석해 결정론적 조건식으로 저장. 이후 매 틱 평가는 LLM 없이 |
| **소급 시뮬레이션** | 규칙 컴파일 시 원장으로 과거 N일 발화 건수를 미리 보여주는 검증 |
| **감시 대상** | 보유 + 관심종목만. 시장 전체 스캔은 안 한다 (`ka10095` 멀티코드 1콜) |
| **자동 집행 없음** | 조건 도달 시 알림까지만. 주문은 사람이 낸다 → **주문 확인 모드**(§1)가 그 지점 |

---

## 11. 프로젝트 운영

| 용어 | 정의 |
|---|---|
| **갈래 A** | MCP · 셸 — `backend/athena_mcp/`, `app/`, `spike/` |
| **갈래 B** | 뇌 · 화면체계 — `backend/athena_api/brain/`, `selector/`, `llm_tools.py` |
| **웨이브 (W0~W5)** | W0 스파이크 / W1 게이트웨이 / W2 Electron 셸 / W2.5 파수꾼 엔진 / W3 카드 계약 / W4 자유 카드·저장 / W5 조사관+규칙 컴파일러 |
| **스파이크 (S1/S2/S3)** | S1 Electron 유리·모션 / S2 Python MCP 클라이언트 왕복 / S3 CLI stream-json 파이프. 완료, 실측 86건 보존 |
| **Ultragoal** | G001~G009 같은 durable story로 구성된 실행 목표 트래킹 단위 |
| **확정된 결정 1~9** | `plan/00-인수인계.md` §3의 되돌리지 않는 결정 목록. 뒤집으려면 근거를 대고 문서를 고친다 |
| **실측** | 문서·README·통념보다 위. 뒤집힌 사례 11건이 `00-인수인계.md` §4에 있다 |
| **함정** | 두 번 빠지지 않기 위해 기록한 목록. `00-인수인계.md` §5 |

### 환경변수

| 이름 | 뜻 |
|---|---|
| `ATHENA_KIWOOM_APP_KEY` / `_SECRET_KEY` | 키움 자격증명. 메모리 전용 |
| `ATHENA_KIWOOM_BASE_URL` | **모의 도메인이 아니면 기동 시 검증 에러** |
| `ATHENA_ENABLE_ORDER_API` | 주문 라우트 활성화(기본 false) |
| `ATHENA_LOCAL_BEARER_TOKEN` | 주문 API·브라우저 WS 인증용 로컬 전용 토큰 |
| `ATHENA_REQUEST_TIMEOUT_SECONDS` / `ATHENA_MAX_RATE_LIMIT_RETRIES` / `ATHENA_MAX_PAGES` | 요청 타임아웃 / 재시도 상한 / 페이지 상한 |
| `ATHENA_MCP_REGISTRY_PATH` | MCP 레지스트리 JSON 경로 재정의 |
| `ATHENA_LADYBUG_DLL_DIR` | Windows LadybugDB 네이티브 DLL 디렉토리 |
| `ATHENA_NO_AUTOSTART` | Electron `main.js`를 라이브러리로 require할 때 자동 기동 차단. **`require.main` 체크의 대체물 — 건드리지 마라** |

---

## 12. ⚠ 충돌하는 말 — 쓰기 전에 확인할 것

| 말 | 충돌 |
|---|---|
| **공통 캔버스 / common canvas** | **갈래 A**: 캔버스 16종(TR 12 + 신규 4)으로 접은 렌더 체계 / **갈래 B**: 키움 301개 라우팅 매핑을 렌더하는 체계. **스케일이 다르다. 어느 쪽이 상위 개념인지 아직 안 정했다.** 렌더러를 건드리기 전에 조율하라 |
| **창** | **창은 둘뿐이다**(대화·캔버스). 설정·주문·온보딩·인증은 창이 아니라 **대화 창의 모드**다. "설정창"·"팝업창"·"일시 표면"은 폐기된 말이다. §1 참조 |
| **캔버스** | **캔버스 창을 뜻한다. 그것 하나뿐이다** (2026-08-17 확정). 창 안의 내용물은 **카드**다 — "캔버스 12종"은 **"카드 12종"**으로 고쳐 부른다. 옛 문서가 내용물을 "캔버스"라 부르는 곳이 남아 있다(§2) |
| **카드** | 캔버스 창 안의 데이터 표현 단위. **12종 고정이고 데이터가 바뀌어도 UI는 안 바뀐다** — Power BI/Tableau의 시각화 컴포넌트와 같은 위치. §2 참조 |
| **완료** | 대화 3상태의 "완료"는 **코드에 별도 상태값이 없다**. `idle` 복귀다 |
| **detail** | "더 가벼운 호출"이 **아니다**. 업스트림 호출량은 base와 같고 응답만 좁아진다 |
| **모델 창 / 설정창 / 설정 모드** | **"모델 창"은 없다** (2026-08-17 확정). 모델 지정은 별도 표면이 아니라 **설정 안에 있는 항목**이다. 표면 이름은 **"설정 모드"** 하나로 통일한다 — "설정창"은 창이 하나 더 있다는 오해를 부른다 (§1) |
| **커맨드바 / 대화 창** | **같은 것이다** (2026-08-17 확정). 대화 창이 곧 커맨드바다. 대화 창 *안의* 어떤 부품이 아니다 |
| **팝업창 / 주문 확인 모드** | 같은 것을 가리킨다. **"주문 확인 모드"로 통일한다.** 주문도 새 창이 아니라 대화 창 위에서 처리한다 (§1) |
