# 열린 질문 (Open Questions)

계획별 미결 항목·사용자 결정 대기 항목을 한 곳에 모은다. 새 계획은 아래에 이어 붙인다.

---

## athena-agent-doctrine-plan — 2026-09-03

- [x] **~~`origin/main` 병합을 어떻게 처리하는가~~ — 닫힘(2026-09-03, 사용자 확정 1).** 답: **병합하지 않는다.** `origin/main`을 기반으로 worktree 브랜치를 딴다 — `git fetch origin && git worktree add .claude/worktrees/agent-dual-control -b feat/agent-dual-control origin/main` 후 하네스 도구 `EnterWorktree {path: '.claude/worktrees/agent-dual-control'}`로 진입한다. 메인 체크아웃의 카드 트랙 미커밋 19파일은 손대지 않으며(별도 체크아웃이라 물리적으로 분리), 병합 충돌은 **발생하지 않는 사건**이 됐다(계획 R13 삭제, R10 강등). 커밋 수 논쟁도 함께 무의미해졌다 — 실측이 초안 37 → Architect 39 → **55**로 계속 변했고, 브랜치를 딴 뒤에는 origin 이동이 이 계획에 영향을 주지 않는다. 브랜치 **통합 시점·방법은 이 계획 밖의 별도 결정**으로 남는다(계획 Follow-ups 7). *(합의 3차 확인: 이 항목은 닫힌 상태를 유지한다. 다만 절차 자체에 실행 불가 결함이 있어 계획 Step 0a~0c를 정정했다 — 복사가 `EnterWorktree` **뒤에** 있어 원본 부재로 첫 명령이 죽던 자리다.)* *(합의 4차 확인: **닫힌 상태 유지.** 절차에 **이탈 규정**을 더했다 — 이 브랜치는 병합하지 않으므로 worktree 디렉터리가 유일한 산출물 보관처다. cwd 복귀는 `ExitWorktree {action:"keep"}`만 쓰고 `action:"remove"`는 부르지 않는다(계획 Step 0a-2 · §6.3). 또한 `git worktree prune`의 근거를 정정했다 — prune은 등록이 끊긴 항목의 **관리 파일만** 지우고 미등록 잔존 디렉터리에는 작용하지 않으므로 "잔존 항목이 add를 방해하지 않도록"이라는 3차 근거는 성립하지 않는다. 커밋 수는 계획 어디에도 박지 않는다 — 개정마다 값이 변했고 브랜치를 딴 뒤에는 origin 이동이 이 계획에 영향을 주지 않는다.)* *(합의 5차 확인: **닫힌 상태 유지 — 재확인 완료.** 절차 자체는 무변경이다. 재검증 실측 `git rev-list --count HEAD..origin/main` = **57**(초안 37 → 39 → 55 → 57)로 또 달라졌고, 이것이 커밋 수를 본문에 박지 않는 판단이 옳았다는 네 번째 증거다. 사용자 확정 1이 전제한 **사실**(origin/main 선행 · `mode` 필드·스냅샷 복원 착륙 완료)은 계획 §1.4 표와 D-3에 코드 실측으로 보존돼 있다. 5차에는 Step 0e의 재해결 대상에 `app/canvas.js`의 **`onOpenGraph:`·`fetchRuns:`·`fetchAvgDuration:`·`fetchEngagement:`** 4이름이 추가됐다 — 이동 8파일에 속하는데 `≈` 없는 확정 좌표(`:3067-3074`)로 남아 재해결되지 않던 자리다(계획 머리말 철회 4).)*

- [ ] **MCP 제안 enum의 모양 — `propose` 중첩(a′-1, 계획 채택) vs 최상위 확장(a′-2, 스펙 문면)** — 스펙 Constraints:55는 "제안 액션 enum(confirm·pause·…)으로 넓히되"라고 적었다. 계획은 `nudge_guard_tools.py:39`(`_ALLOWED_ACTIONS = ("propose","get")`, 실측)와 같은 모양(`action: draft|list|propose` + `propose.control` enum)을 택해 기존 회귀 테스트 `test_state_changing_actions_are_gateway_blocked`를 살렸다(`test_routine_tools.py:37-50`이 최상위 action만 검사하므로 중첩은 이 단언을 건드리지 않는다). 검토자가 문자 그대로를 원하면 되돌리는 비용은 `routine_tools.py` 1파일 + `test_routine_tools.py` 단언뿐이다. — **계획 Step 5 검수 게이트의 확인 항목 U-2로 승격됐다.**

- [ ] **`symbol`·`condition.source`를 설정 편집에서 제외하는 것이 "모든 설정 편집"을 만족하는가** — 계획은 이 둘을 422로 금지한다(실시간 구독 정체성이 바뀌면 `ensure/release` 왕복 설계가 필요해 최소 범위를 넘는다). D2는 "모든 요소를 제어"라고 했으므로 사용자 확인이 필요하다. — **계획 Step 5 검수 게이트의 확인 항목 U-1로 승격됐다**(사용자 확정 4: 조용한 축소가 아니라 명시적 확인 항목으로 올릴 것). ⚠ **합의 4차 대상 정정:** U-1이 묻는 대상을 스펙 문면에 맞췄다 — 스펙 A 인벤토리의 편집 대상 열거는 `조건·모드·소스·쿨다운·브리핑 모델`이라 **`종목`은 애초에 스펙 범위가 아니고**(제외해도 축소가 아니다), 반대로 **스펙 범위인 `모드`를 3차 U-1이 묻지 않았다.** 이제 U-1은 `condition.source`(그리고 그로부터 유도되는 `모드`)를 대상으로 한다. `종목`은 편집 대상이 아니되 화면에는 **읽기 전용 표기로 남는다.** ⚠ **합의 6차 문면 정합(본문과 미결 목록이 다른 값을 말하지 않게 한다 — 계획 철회 3이 세운 기준):** 5차 본문 ①-7이 「정확히 3건」을 **합집합 등호**로 교체했으므로 여기서도 개수를 쓰지 않는다. 확정 문면은 — 판정 주어는 **상세 패널의 설정 폼**이고, **루틴별로** `그 폼이 낸 모든 라벨 == (편집 가능 필드 라벨) ∪ (읽기 전용 표기 라벨)`, 두 집합은 서로소이며, 읽기 전용 변은 **`지정 라벨 6종(`종목`·`모드`·`소스`·`다음 실행`·`생성`·`만료 <날짜>`) ∩ 그 루틴에 실제로 존재하는 필드`**다(합의 6차 — 감시 루틴에는 `다음 실행`이 존재하지 않으므로 6종을 상수로 두면 올바른 구현이 반려된다). 기본 안내는 **"취소 후 새로 만들기"**이며, 이 문구는 06 보드(계획 Step 3-3)와 설정 패널(Step 7a)에 **실제 문자열 `종목·소스는 취소 후 새로 만들기`로 들어간다** — 상태 표기만 두고 안내를 빠뜨렸던 초안 상태를 고쳤다.

- [ ] **"보류"(C)가 서버에 남지 않아도 되는가** — 제안은 `GET /api/v1/brain/profile-summary`가 매번 새로 계산해 내려주고, 채택/보류를 기록하는 엔드포인트가 없다(`routines.py`의 라우터 12개에 해당 경로 없음). 계획은 보류를 렌더러 로컬 숨김으로 두고 `보류함 N건`으로 정직하게 표기한다 — **계획 전체에서 IPC를 타지 않는 유일한 동작**이다. 앱을 다시 켜면 같은 제안이 되살아난다. — **계획 Step 5 확인 항목 U-4로 승격.** 영속이 필요하면 백엔드 계약 추가가 선행돼야 하며 이 계획의 범위 밖이다(Follow-ups 8).

- [ ] **`미리보기 실행` 칩을 계속 비활성으로 두는가** — `app/chat.js`가 "백엔드에 대응 엔드포인트가 없다"는 이유로 비활성으로 둔다(origin 실측 `≈:3014-3018`, 줄 번호는 계획 Step 0e에서 재해결). 07 보드(제어 제안 턴)에도 같은 비활성 상태를 그릴지, 아니면 칩 자체를 빼고 `바로 활성화`만 남길지. — 죽은 버튼 금지(P3)는 둘 다 만족한다. **계획 Step 5 확인 항목 U-3으로 승격**(기본안: 07 보드에는 그리지 않는다).

- [ ] **~~펜 시트·~~같은 모드 창 **동시 상주 세션**은 언제 누가 만드는가**(합의 3차에 제목·범위 정정) — 공통 세션 기반 조각 중 **유일하게 미소유**로 남은 것이다. `docs/plans/session-persistence-spec.md:89-93`이 **Athena 데몬**(화면 39·43)을 선행 조건으로 지목하며 데몬은 미착수다. ~~이 계획은 "관제 창은 최신 1건" 전제로 우회하고 테스트로 그 전제를 고정한다.~~ — **합의 3차에 범위가 좁아졌다.** 실측으로 (a) **펜 시트는 origin에 이미 착륙했고**(`app/lib/sidebar-project-menu.js`의 `MODE_CHOICES`에 `{mode:'agent'}`, `sidebar.js ≈:333·349·901`, `session-persistence-spec.md:182`가 `[x]`), (b) `mode==='agent'` **대화 레코드 다건도 이미 만들어진다**(`conversations.js function touch` `≈:198`가 개수 제한 없이 push, `≈:213-221`). 따라서 계획은 "최신 1건" 전제를 **폐기**하고 `getAgentConsoleSessionId()`를 **"활성 대화가 에이전트면 그 id, 아니면 null"**로 재정의했다(계획 §1.4·②-5·R15). 이 계획 밖으로 남는 것은 **동시 상주 세션**뿐이며(`session-persistence-spec.md:130` "상주 채팅 세션은 프로세스 전역 하나"), `:89-93`이 그 소유자를 미착수 **Athena 데몬**(화면 39·43)으로 지목한다. 별도 트랙이 필요하다.

### 합의 3차에 추가된 미결 (2026-09-03)

- [ ] **`propose.control`에 `update`를 더해 12종으로 두는 것이 스펙 범위 유지인가** — 스펙 Constraints:55의 열거는 10종이고, 계획은 `ack_all`(스펙 B 인벤토리 "모두 읽음")에 이어 **`update`**(스펙 A 인벤토리 "설정 편집")를 더한다. 근거: 2차처럼 그 자리를 `chipToIpc('confirm')`으로 메우면 `POST /{id}/confirm`(`routines.py:211-229` = `status→active` + `ensure_realtime_subscription`)이 불려 **칩이 자기 글자와 다른 일을 한다**(P3). 대안은 "A-설정편집의 대화 경로 미지원 선언"뿐이고 그것은 D2 위반이다. — **범위 확장이 아니라 스펙 인벤토리와 Constraints 열거 사이의 누락 보정**이라는 것이 계획의 판정이며(계획 §1.3 출처 표), Step 5 검수 게이트에서 U-2와 함께 확인받는다.

- [ ] **06 설정 폼의 소스별 분기를 사용자가 승인하는가** — **`models.py:42-51 SOURCES`**(합의 6차 좌표 정합 — 5차 본문·실측 모두 `:42-51`인데 이 줄만 정정 전 `:43-52`가 남아 전파가 부분적이었다)가 number/bool/string 세 갈래이고 연산자 집합이 소스마다 달라(`_NUM_OPS`에 `==`가 **없다**), 폼은 `spec.ops`·`spec.value_type`·`spec.transport`에 따라 모양이 바뀐다. 그 결과 AC가 "입력 정확히 6개"에서 **집합 동일성**으로 바뀌어 셀 수 있는 개수 기준을 잃는다(대신 `SOURCES` 6종 테이블 구동 테스트로 단언은 강해진다). — 06 보드가 대표 2종(`price.current`·`schedule.daily`)을 나란히 그려 **Step 5 PDF 검수에서 눈으로 승인**받는다. 분기하지 않으면 `schedule.daily`·`vi.triggered` 루틴은 GUI 편집이 **전부 422**다.

- [ ] **`--color-k-faint`·`--color-k-dim` 토큰이 Paper `get_tokens`에 실재하는가** — 실재하면 Step 2 규율에서 hex(`#626B76`·`#5B6270`)를 지우고 토큰명만 남긴다. 없으면 "기존 결정의 계승이며 AC ①-10 `하드코딩 hex 0` 검사의 명시 예외"로 노트에 기록한다. — **Step 0f가 실행 중 확인해 노트에 남긴다**(사람 결정 불필요, 기록만 남기면 된다).

### 합의 4차에 추가된 미결 (2026-09-03)

- [ ] **조건 술어(`비교`·`값`·`연속 틱`) 편집을 GUI 전용으로 두는 것이 D2를 만족하는가 — 계획 Step 5 확인 항목 U-5** — 계획 결정 (d). 실측으로 `_view()`(`backend/athena_api/api/routines.py:111-142`)가 *"조건 원문 dict 대신 사람이 읽는 해석문만 노출한다"*를 독스트링으로 선언하며 `condition`을 통째로 빼고 `source_label`(`"현재가"` 같은 한국어 라벨)만 낸다. 모델이 도달하는 읽기는 그 목록 하나뿐이므로, 대화 경로가 `op`/`value`를 제안하려면 **한국어 라벨에서 소스 키를 역추론**해야 한다(P3 위반 · 계획 R20 재발). 스펙 A 인벤토리는 편집 대상에 `조건`을 명시하므로 이것은 **대화 경로의 부분 축소**이고, 사용자 확정 4에 따라 조용히 넘기지 않는다. **기본안:** 조건 술어는 GUI 전용, 대화는 공통 5필드(`쿨다운`·`설명`·`만료`·`브리핑 모델`·`브리핑 노력`)만 제안하고 조건 요청에는 `control:'view'`로 설정 탭을 연다. **대안(반려 시):** 결정 d-1로 바꿔 `_view`에 `condition`을 실어 모델도 술어를 보게 한다 — 코드 변경은 `_view` 1곳 + 단언 1건으로 작지만 **모델 도달 읽기 표면이 실제로 커진다**(계획 D-1). ⚠ **합의 5차 근거 정정 — 이 항목의 전제가 사실과 어긋나 있었다(계획 머리말 철회 5).** 위 문단의 *"모델이 도달하는 읽기는 목록 하나뿐이므로 대화 경로가 `op`/`value`를 제안하려면 한국어 라벨에서 역추론해야 한다"*는 **절반만 참이다.** 실측 — `backend/athena_api/routines/rules.py:168-169`가 `if not note: spec.note = spec.human_summary()`로 note 미지정 루틴(기본 경로)의 `note`를 채우고, `models.py:168-179 human_summary()`가 `f"{symbol} · {label} {op} {value} · 방식 {mode}"`를 만들며, `_view`(`routines.py:122`)가 그 `note`를 그대로 내보낸다. 즉 **모델은 `op`·`value`를 산문으로 이미 읽고 있다.** 모델에게 가지 않는 것은 **기계 판독 소스 키(`price.current`)와 연산자 집합**(`models.py:22-25,42-51`)이며, 그것이 "모델이 **유효한 술어 패치를 조립할 수 있는가**"를 가른다. **정확한 질문은 "모델이 술어를 보는가"가 아니라 "모델이 유효한 술어 패치를 조립할 수 있게 할 것인가"다** — 승인/반려의 실질이 달라지는 자리라 문면을 고쳤고, 결정 d-2 자체는 유효하므로 기본안은 그대로다.

- [x] **~~조건 원문을 어디서 읽는가~~ — 닫힘(2026-09-03, 합의 4차 결정 d-2).** 3차 계획은 쓰기(`POST /{id}/update`)만 신설하고 읽기 계약을 한 줄도 건드리지 않아 06 설정 폼과 07 A열 제안이 **두 경로 모두에서 구현 불가**였다(단일 루틴 GET 라우트가 없고, `GET /{id}/runs`도 spec을 안 내며, 렌더러는 `item.raw`만 읽는다). 답: **신규 `GET /api/v1/routines/{routine_id}`를 렌더러 전용 상세로 만든다** — `_view` 결과 + `condition` 블록. `_view`(모델 도달 표면)는 **그대로 동결**하고, `test_propose_only_reads_list`가 허용 path 집합을 `{"/api/v1/routines"}` **등호**로 고정해 모델을 차단한다. 기각한 대안: (d-1) `_view` 확장 — 모델 표면이 함께 넓어진다 / (d-3) `/runs` 동봉 — 이력 엔드포인트가 명세를 내는 의미 왜곡. **새 위험 1건을 함께 닫았다** — FastAPI는 선언 순서로 매칭하므로 신규 라우트를 `@router.get("/briefing-budget")` **뒤에** 선언해야 하고(계획 R26), 회귀 단언 1건(②-9b ④)이 이를 고정한다.

- [x] **~~A-확정의 GUI 경로가 없는데 규칙 ③이 버튼을 약속한다~~ — 닫힘(2026-09-03, 합의 4차).** 계획이 확정 문구로 못 박은 이중 제어 규칙 ③ `확정 — 채팅 칩으로도, 버튼으로도`는 Paper·앱·테스트·프로브의 단일 원본인데, 3차의 어느 Step도 그 **버튼**을 만들지 않았고 상세 패널 버튼은 3개(`일시중지`/`재개`/`취소`)로 단언돼 있었다. 문자열 단언과 프로브 원문 단언은 **전건 통과**하므로 자동 게이트가 이 거짓말을 잡지 못한다. 답: **버튼을 만든다** — `athena:routine-confirm` 채널·핸들러·백엔드가 전부 이미 있고 캔버스 호출자만 없었으므로 비용은 콜백 1개다(`status==='draft'` && `activation_blocker` 없음일 때만 활성). 아울러 `app/lib/agent-canvas.js:22-27`이 *"상세 패널은 draft 항목에서 읽기 전용(액션 버튼 없음)"*을 명시적 계약 주석으로 적어 두었으므로, 그 주석을 새 계약으로 **함께 갱신**한다(말없이 어기지 않는다).

### 합의 5차에 추가된 미결 (2026-09-03)

- [ ] **GUI 설정 편집 폼을 드릴인 `설정` 탭에서 「상세 패널」로 옮기는 것이 맞는가 — 계획 Step 5 확인 항목 U-6** — 계획 결정 (f). 실측으로 드릴인 진입 버튼 `전체 이력 보기 →`(`app/lib/agent-canvas.js:1536`, `openHistory` `:1063`)는 **바깥 게이트 `if (item.kind !== 'draft')`(`:1525`)와 안쪽 게이트 `if (item.kind === 'watch')`(`:1533`)를 모두 통과할 때만** 그려지고, `설정` 탭은 드릴인 전용이다(`:298-299` 주석). 파일 머리말 `:59-62`가 *"draft·예약(schedule)은 이 링크가 없다"*를 계약으로 적어 뒀고 `allItems()`(`:1294-1332`)가 `mode==='scheduled'`→`kind:'schedule'`, `status==='draft'`→`kind:'draft'`로 보낸다. 즉 4차 명세대로면 **예약·초안 루틴에는 GUI 설정 편집 경로가 존재하지 않아** D2("GUI도 모든 제어")가 조용히 축소되고, 9c 2행(예약 루틴의 설정 폼)이 물리적으로 실행 불가다. 화면 계약이 바뀌는 일이라 사용자 확정 4에 따라 확인 항목으로 올린다. **기본안:** f-2 — 폼을 `renderDetail` 상세 패널(세 kind 전부에서 그려지고 4차가 `확정` 버튼을 넣기로 한 그 자리)로 옮기고, 드릴인 `설정` 탭은 **읽기 전용 요약 + `설정 편집`** 버튼(누르면 드릴인이 닫히고 상세 패널 설정 폼 첫 입력으로 포커스가 간다 — 합의 6차 동작 확정, 계획 §1.8 부록)만 남긴다. **대안(반려 시):** f-3 — "GUI 설정 편집은 감시 루틴 전용"을 선언한다. 그 경우 06 보드가 그리는 `schedule.daily` 폼에 **앱 대응물이 없어져** 보드 문구를 함께 고쳐야 하고 9c 2행의 시연 루틴을 감시 루틴으로 바꿔야 한다. 기각한 대안: f-1(`kind==='schedule'`에도 드릴인 링크) — 바깥 게이트 때문에 **초안이 여전히 남고**, 두 게이트를 다 열면 draft 드릴인이 빈 "최근 실행"을 열어 `:1525` 주석이 지킨 계약(*"빈 로그를 지어내 보여주지 않는다"*)을 깬다.

- [x] **~~폼이 제시할 선택지(`ops`·`value_type`·`transport`)를 렌더러가 어디서 얻는가~~ — 닫힘(2026-09-03, 합의 5차 결정 e-1).** 4차 계획은 폼 모양 전체를 `spec.ops`·`spec.value_type`·`spec.transport`로 결정하라 지시했지만 **앱 전체에 소스 카탈로그가 0건**이었다 — `grep -rn "price.current\|source_spec\|value_type\|spec.ops\|SOURCES" app/lib/*.js app/canvas.js app/chat.js`의 유일한 히트가 **`app/chat.js` `≈:2949`**(앵커는 좌표가 아니라 `SOURCES 카탈로그` **문자열** 자체다)의 주석 한 줄이고, 4차의 `GET /{id}`는 `condition` 4키만 준다. ⚠ **합의 6차 좌표 정정:** 5차가 여기 적은 `:2524`는 **로컬 `HEAD` 좌표**였다(실측 `git show origin/main:app/chat.js | grep -n "SOURCES 카탈로그"` → **2949** vs `git show HEAD:app/chat.js | …` → **2524**) — 결론(앱 카탈로그 0건)은 origin 실측으로 참이므로 **결정 e-1은 유효하고 고친 것은 좌표뿐이다**(계획 §1.7 표 · 머리말 철회 3·4와 같은 실패 모드). `mode`에서 `transport`는 유도되지만 **`ops`·`value_type`은 유도 불가**다(`price.current`와 `vi.triggered`가 둘 다 `realtime-ws`인데 `_NUM_OPS`/number vs `_EQ_OPS`/bool로 갈린다 — `models.py:22-25,42-51`). 답: **서버가 실어 보낸다** — `GET /api/v1/routines/{routine_id}` 응답에 `source_spec = {ops, value_type, transport, label}`를 더하고, routine_id가 없는 05 `＋ 새 작업` 시트를 위해 **`GET /api/v1/routines/source-catalog`**(활성 `SOURCES` 6종)를 신설한다. 둘 다 렌더러 전용이라 `test_propose_only_reads_list`의 허용 path 집합 밖이고 **모델 도달 표면은 무변경**이다. 기각한 대안: (e-2) 렌더러 사본 + 발산 단언 — 두 벌 표면이 6소스 × 4속성이라 채널명 12개보다 훨씬 크고 실패 모드가 런타임 422라 늦게 드러난다 / (e-3) 하드코딩 + 사용자 확인 — **카탈로그는 사용자 선호가 아니라 코드 사실**이라 U 항목의 성격이 아니다. **판단 기준은 "사본이 나쁜가"가 아니라 "사본이 불가피한가"다** — 채널명은 두 렌더러가 각자 부르므로 불가피해 결합 단언(8a-2)으로 지키고, 카탈로그는 서버가 유일 소유자라 사본을 만들지 않는다.

- [x] **~~설정을 편집하면 `note`가 옛 술어인 채로 남는가~~ — 닫힘(2026-09-03, 합의 5차 R30).** 실측 — `to_dict()`(`models.py:181-196`)가 기존 `note`를 그대로 싣고 `rules.py:168-169`는 **note가 빈 경우에만** `human_summary()`로 재생성하므로, note를 지정하지 않고 만든 루틴(기본 경로)의 조건을 편집하면 옛 술어 문장이 남는다. 그 `note`는 감시·예약·초안 **모든 목록 행의 제목**(`app/lib/agent-canvas.js:1285`·`:1299`·`:1313`)이고 설정 표면이 `['조건', r.note]`로 조건이라 라벨하는 값이며(`:1000`) `_view`(`routines.py:122`)가 내보내 **모델이 읽는 유일한 루틴 설명**이다 — 조건을 고치면 화면과 모델이 함께 옛 조건을 말한다. 답: **신선도 규칙 2개** — 편집 전 `spec.note == spec.human_summary()`이면(= 자동 생성분) `condition`이 실제로 바뀐 경우에 한해 `note`를 비워 재생성하고, **사용자가 직접 쓴 `note`는 절대 덮지 않는다.** 계획 ②-9의 11번째 케이스가 두 분기를 고정하고, Step 9b가 시연 루틴 2건의 `note`를 서로 다르게 심어(예약 = 자동 생성, 감시 = 명시) 두 분기를 라이브에서 모두 도달시킨다.

- [x] **~~신규 `GET /{routine_id}`가 무엇을 반환하는가~~ — 닫힘(2026-09-03, 합의 5차 R33).** 4차는 *"`_view(spec, runtime)` 결과 + `condition` 블록"*이라 적었는데, `_view(spec, runtime, *, latest_fired=None)`(`backend/athena_api/api/routines.py:108-110`)의 `latest_fired`는 `list_routines()`가 ledger를 1회 스캔해 만든 맵에서 주입하는 값이라 상세 라우트에는 없다. 그대로 구현하면 발화한 루틴에도 `last_fired_at: null`이 나가고, 더 나쁘게 **예약 루틴은 `missed`가 항상 `true`**가 된다 — `_is_missed(missed_at, None)`이 `last_fired_at is None`에서 `return True`(`:79-80`)이기 때문이다. 계획 Step 7a가 `routine.missed === true`를 `지금 실행` 활성 조건으로 쓰므로(R7) **409를 부르는 거짓 활성 버튼**이 생긴다. 답: **응답을 폼 프리필 키로 좁힌다** — `id`·`symbol`·`status`·`mode`·`source_label`·`cooldown_s`·`expires_at`·`note`·`briefing_model`·`briefing_effort`·`activation_blocker`·`experimental_source`·**`goal`** + `condition` + `source_spec`이며, `last_fired_at`·`unread`·`missed`·`next_fire_at`·`created_at`·`approved_at`은 **내지 않는다**(P3 — 읽어올 수 없는 값을 선언하지 않는다). ⚠ **합의 6차 `goal` 편입:** 5차 목록은 이 키를 **포함하지도 「내지 않는다」에 넣지도** 않았는데 `_view`는 실제로 낸다(실측 origin `backend/athena_api/api/routines.py:123` `"goal": spec.goal`) — `to_dict()`가 `goal`을 싣고(`models.py:189`) `last_fired_at` 계열과 달리 **`latest_fired` 주입 없이도 참인 값**이라 **R33의 축소 사유가 적용되지 않는다**(계획 §1.6 ⚠ 문단과 같은 사유). ②-9b ①이 반환 키 집합을 **등호로** 단언하므로 이 열거가 곧 계약이다. 기각한 대안: 상세 라우트가 최신 fired 행 1건을 조회해 `latest_fired`를 주입한다 — 폼 프리필에 그 세 값이 필요 없고 폼을 열 때마다 이력 조회 비용이 든다.

### 합의 6차에 추가된 미결 (2026-09-03)

- [x] **~~드릴인 `설정` 탭의 `설정 편집` 버튼이 어디로 데려가는가~~ — 닫힘(2026-09-03, 합의 6차).** 5차는 `설정 편집 ↓`를 **"상세 패널로 스크롤"**로 정의했는데, 실측 — `detailCol`은 `panels`에(`app/lib/agent-canvas.js:498`), `panels`는 `body`에 붙고(`:499`) **`openHistory(item)`가 `body.hidden = true`(`:1076`)로 그 트리를 통째로 숨긴다**(`closeHistory` `:1104`). 즉 드릴인이 열려 있는 동안 상세 패널은 **렌더되지 않아** 스크롤 대상이 화면에 없다. f-2가 **새 죽은 링크**를 하나 만든 것이고, 8c의 5차 단언(*"요약 + 그 버튼이 있다"*)은 그것을 **전건 통과**시켰다(R29·R32와 같은 계열이 그 계열을 닫은 라운드에서 재발). 답: **버튼은 `closeHistory()` → 항목 선택 → 상세 패널 설정 폼 첫 입력 포커스**이고, 라벨에서 `↓`를 뺀다. 8c 단언도 존재가 아니라 **동작**(`body.hidden === false` + 입력 ≥1 + 포커스)을 본다. — **U-6의 기본안 자체는 무변경**이므로 사용자에게 새로 물을 것은 없다.

- [x] **~~`확정` 비활성 사유 문구를 백엔드 원문으로 쓰는가~~ — 닫힘(2026-09-03, 합의 6차).** 5차는 `activation_blocker` **원문 그대로**를 그리기로 했는데, 실측 `can_activate`(`backend/athena_api/routines/runtime.py:71-85`)의 반환값 셋은 전부 **`~없다` 종결 완결 설명문**이고 `:78`·`:85`는 **영문 내부어 `source`**를 포함한다 — P5의 「설명문 금지」·「내부어 금지」를 동시에 어기는데 ①-10의 정규식이 `~없다`를 못 잡아 **게이트가 전건 통과하면서 화면이 규율을 어긴다.** 게다가 레거시 소스 루틴 한 상태에 `<blocker>` 원문과 `이 작업은 취소 후 새로 만들기` **두 문구**가 배정돼 있었다(P3). 답: **`activation_blocker`는 신호로만 쓰고(non-null = 비활성) 문구는 제품이 소유한다** — 레거시 소스 → `이 작업은 취소 후 새로 만들기` / WS 미가용 → `실시간 감시 불가 — 키움 연결 없음` / 실행 경로 없음 → `백엔드 실행 경로 없음` / 미지 blocker만 원문 폴백 + 노트. 이 계획의 `missed → 놓친 예약 없음`과 같은 취급이며, e-1(카탈로그 사본 금지)과 충돌하지 않는다 — e-1이 막는 것은 **유효성을 정하는 카탈로그의 사본**이지 표시 문자열이 아니다. — **세 문구는 Step 5 PDF 검수에서 눈으로 승인받는다**(문구 3원칙 판정은 grep으로 못 닫는다).

- [ ] **`isDerivedAlertRoom`의 소유 모듈 이동이 §1.4 「어댑터 2함수」 서술과 어긋나지 않는가** — 합의 6차에 이 함수를 main 모듈 `app/lib/main/agent-session.js` → 렌더러 UMD `app/lib/agent-proposal.js`로 옮겼다(렌더러는 `shell.html`의 `<script src="lib/*.js">`로만 모듈을 얻고 그 목록에 `lib/main/*`이 **0건**이라 유일한 호출자가 import할 수 없었다 — 실행자에게 남는 선택지가 **사본 두 벌**뿐이었다). 결정 (b)의 「최소 어댑터」 취지는 그대로이고 함수 총수도 2로 같지만, `agent-session.js`가 소유하는 것은 이제 1함수 + `FOUNDATION_PRESENT`다. — **코드 사실에 따른 배치 정정이라 사용자 결정이 필요하지 않다.** Step 6f·7c가 실행 중 확인하며, 어긋나면 `app/main.js:29`(`require('./lib/backtest-spec')`)의 선례대로 main이 렌더러 UMD를 `require`한다.

## 플러그인 모드 기조 정렬 — 합의 계획 (반복 6) - 2026-09-03

- [x] **~~그래프 모드 표면을 만지는 예외를 승인하는가~~ — 닫힘(2026-09-03 사용자 결정: 예외 **승인**, `#chatModeHead`를 공용 모드 헤더로 일반화, 그래프 문구·동작 회귀 단언 3종 고정 — 계획 §1에 기록).** 원문: 그래프 모드 표면을 만지는 예외를 승인하는가 — `.omc/plans/athena-plugin-doctrine-consensus.md` §1의 `【사용자 결정: 그래프 헤더 예외 — 승인 / 거부】`** — W3-6이 `app/shell.html:296-299`의 하드코딩 채팅 헤더 2줄을 controller 주입으로 바꾸고 `app/lib/graph-mode/controller.js:302`의 가시성 조건을 `graphView || pluginView`로 넓힌다. 이것이 수락 기준 ①-03("아테나 · 플러그인 대화" 헤더)을 만족시키는 가장 짧은 이음매이지만 제약 "타 모드 불간섭"의 예외이고, 반복 5까지 §1은 "승인된 예외", W5-4 ⑷는 "승인 기록 없음"으로 **자기모순**이었다(Critic RC-4). 반복 6에서 §1을 "승인 상정"으로 바꾸고 거부 시 폴백(플러그인 소유 `#pluginChatHead`, 그래프 코드 0줄 · 회귀 단언 3종)까지 적어 두었다. — **ralph는 합의 직후 착수하므로 이 한 줄이 비어 있으면 §1만 읽고 승인된 것으로 간주해 그래프 표면을 만진다. 계획 확정 시 반드시 채운다.**
- [ ] **W5-4 PDF 검수 안건 5건** — ⑴ 09번 하단 경계 문구 `앱을 완전히 껐다 켜면 대기 중인 제안은 사라집니다`(문구 3원칙의 설명문 금지와 마찰) ⑵ GUI 경로가 채팅 제안 턴을 만들지 않는 결정(명세 R4 원문과 다른 제품 결정) ⑶ 이미 검수를 통과한 `verify.js:2104` subtitle 폐기 ⑷ 위 그래프 헤더 예외 ⑸ 모드 밖 폐기 문구 `플러그인 모드에서 다시 요청합니다`. — 전부 grep으로 못 잡는 제품 문구·범위 판단이라 PDF 검수에서 사용자 승인을 받는다.
- [ ] **Paper 보드 6장의 실제 노드 구조·현재 문구·아트보드 치수** — 계획 단계에서 Paper MCP 호출이 금지돼 확인하지 못했다. W4 첫 호출 `get_basic_info`로 대조하고 어긋나면 계획의 좌표·id를 실측으로 교체한 뒤 진행한다(확정 7). 중단하지 않는다.
- [ ] **R7 플레이키 테스트의 정체** — 같은 커밋에서 1회차 1 fail, 2회차 0 fail이 관측됐으나 어느 테스트인지 미확정. §7-1의 2회 실행 `not ok` 집합 대조가 이름을 드러내게 했고, 판정은 "2회 연속 통과"로 흡수한다.
