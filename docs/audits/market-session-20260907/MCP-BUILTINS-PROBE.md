# MCP built-ins protocol probe

Status: **PASS_PROTOCOL_METADATA_ONLY — ONE REVIEWED ISOLATED STDIO EXECUTION COMPLETED**

This probe starts Athena MCP only with a newly allocated absolute state directory and a newly allocated explicit registry path under `artifacts/market-session-audit/2026-09-07/mcp-builtins-probe/`. Relative inputs are rejected before resolution. The registry filename `mcp_servers.json` is rejected, all three state/registry/output paths must share one wholly absent run root while remaining distinct and absent, and paths outside the owned audit root are rejected. The user's default registry, consent store, logs, and canvas directory are never selected.

The child command is fixed to the repository backend virtual environment and the exact argument form `python -m athena_mcp --state-dir ABS_OWNED --registry ABS_OWNED_NEW serve`, with the new worktree's backend directory as cwd. The installed MCP SDK merges its default inherited environment with the supplied server environment. The probe therefore overrides `APPDATA`, `LOCALAPPDATA`, `USERPROFILE`, `HOMEDRIVE`, `HOMEPATH`, `USERNAME`, `TEMP`, and `TMP` with synthetic locations under the absent owned run root, sets `PYTHONDONTWRITEBYTECODE=1`, and rebuilds `PATH` from only the selected backend venv and Windows system directory. Before spawn, the merged effective environment is checked for host-profile paths and credential-shaped keys; the synthetic roaming/local profile and temp directories are then created under the owned run root before the stdio context is entered. Thirteen Athena source files and the exact installed MCP SDK stdio source file must match frozen SHA-256 values before the owned run directory is created or stdio is opened. A missing registry loads as an empty registry and an empty consent store has no approved providers, so this run authorizes zero provider processes.

The MCP SDK interaction is limited to `ClientSession.initialize()` followed by `ClientSession.list_tools()`. There is no `call_tool` branch. The pinned SDK stdio context closes stdin, waits for graceful exit, then uses its Windows process-tree termination path if needed. The report separately records entry and successful exit of the SDK stdio and session contexts and passes only when both exit. This is SDK-context cleanup evidence. The probe does not capture a child PID and does not independently verify OS process or descendant exit; both limitations are explicit report fields. It does not enumerate or kill unrelated processes and does not retry the previously rejected recursive temporary-directory cleanup.

The expected protocol metadata is exactly 12 Athena built-ins, including `athena__render_canvas` and `athena__save_canvas`, with 58 action enum values across the current input schemas. Artifacts retain counts, boolean matches, and SHA-256 fingerprints of the sorted name set and name-plus-input-schema rows. Tool descriptions, schema bodies, registry contents, tool results, credentials, and provider output are not persisted.

`PASS_PROTOCOL_METADATA_ONLY` proves one isolated stdio initialize/list-tools exchange and successful exit from the pinned SDK contexts. It does not prove independent OS PID/descendant cleanup, the user's running MCP registry, user consent state, provider connectivity, backend data, or any tool execution. The whole-day application audit remains `PARTIAL`.

The frozen probe and its injected-context tests passed independent review after 15/15 mock-only tests. It was then executed once at `2026-09-07 13:46:14 KST` (`generated_at: 2026-09-07T04:46:14.398130+00:00`) against the unique absent run root `run-20260907T134502KST`. The parent process exited `0` and stdout reported `PASS_PROTOCOL_METADATA_ONLY`. The executed command included `-B`:

```powershell
& 'C:\Projects\DAOU.Athena-market-audit-20260907\backend\.venv\Scripts\python.exe' -B 'C:\Projects\DAOU.Athena-market-audit-20260907\scripts\market-session-audit\mcp-builtins-probe.py' --execute --state-dir 'C:\Projects\DAOU.Athena-market-audit-20260907\artifacts\market-session-audit\2026-09-07\mcp-builtins-probe\run-20260907T134502KST\state' --registry 'C:\Projects\DAOU.Athena-market-audit-20260907\artifacts\market-session-audit\2026-09-07\mcp-builtins-probe\run-20260907T134502KST\registry-new.json' --output 'C:\Projects\DAOU.Athena-market-audit-20260907\artifacts\market-session-audit\2026-09-07\mcp-builtins-probe\run-20260907T134502KST\report.json'
```

The resulting `report.json` records one initialize and one list-tools completion, zero `call_tool` calls, zero authorized provider processes, no user-registry read, and successful exit from both pinned SDK contexts. It observed 12 tools, 2 canvas tools, and 58 action enum values; the exact name set, schema fingerprint, and expected action count all matched. The owned state, registry, profile, and temp checks all passed.

This actual result remains protocol-metadata evidence only. The report records `user_runtime_proven: false`, `upstream_connectivity_proven: false`, `os_process_identity_captured: false`, and `os_process_exit_independently_verified: false`. It does not prove provider connectivity, backend data, individual tool execution, the user's runtime registry, or complete application behavior. The whole-day application audit remains `PARTIAL`.

Unit tests use injected async contexts and sessions. They assert the exact command and paths, synthetic environment directory existence before transport entry, initialize/list-only behavior, 12/2/58 fingerprints, fail-closed source and path checks, timeout cleanup, zero tool calls, and absence of descriptions/schema bodies in the report. They do not spawn the real stdio server.
