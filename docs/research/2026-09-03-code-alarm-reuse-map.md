# 코드 알람 — 재사용 지점 실측 지도 (2026-09-03, Explore 2건 요약)

worktree `feat/agent-dual-control` 기준. 줄 번호는 실측 시점 참고값, 이름이 계약.

## A. 백테스트 파이프라인 (재사용 대상)

### 스펙·코드 생성
- `backend/athena_api/backtest/schema.py` — `StrategySpec{version, metadata, data?, strategy, risk, costs?}` (:180). `DataSpec{symbols, period: day|week|month, adjusted, from, to}` (:55) — 분/틱 없음. `StrategyBlock{params: {name: ParamSpec}, indicators, entry, exit}` (:126). `ConditionSpec{indicator, operator(7종), compare_to}` (:99). 코드 경로용 `_RelaxedStrategySpec` (:214, entry/exit 조건 비어도 됨; `require_conditions=code_source is None` api/backtest.py:334).
- `codegen.py` — 생성 산출물 계약: `PARAMS = {...}` 최상위 리터럴 + `def signals(df, p)` (:177, :304) → `df.assign(entry=..., exit=...)[["entry","exit"]]`. `spec_to_python(spec)` (:140). 그래프 경로 `generate_from_graph(graph, filename)` (:568) → `{source, spec, hashes, source_map{entries:[{node_id, ast_path, role, source_span}]}}`, `preview_from_graph` (:613), `attach_spans` (:662), `_GraphEmitter` (:262) `_emit_outputs` — `output.entry/output.exit`만 안다.
- df 열: `open high low close volume` + 오름차순 DatetimeIndex (compile.py:24, indicators/registry.py:35, api/backtest.py:113 `_candles_to_frame`).
- 지표 25종 `sandbox/api.py` `bt.*` (:29-88): sma ema wma dema tema rsi macd stoch cci willr roc mom adx aroon sar atr natr bbands kc donchian std obv mfi vwma cmf + cross_above/cross_below.
- `user_strategies.py` — 등록부(코드 저장소 아님): `UserStrategy{id,name,project_id,path,created_at}` (:26), `UserStrategyRegistry` (:73), `.py`만·폴더 밖 금지 (:55). 파일이 진실(D2).

### 샌드박스
- `sandbox/host.run_strategy(jobdir, strategy_source, bars: DataFrame, params, *, timeout=30.0, stdout_cap_bytes=64KB, python_exe=None, allowed_imports=None)` (:101) → `{ok, signals_df, stdout, error{type,message,traceback}, elapsed}`. 프로토콜: jobdir에 `spec.json`·`bars.csv`·`strategy.py` → `python -s -P -B -m athena_api.backtest.sandbox <jobdir>` (:138-144) → `signals.csv` 또는 `error.json`.
- 경계 4종(docstring :1-11): env 화이트리스트 `_ENV_ALLOWLIST`(:31 PATH/PYTHONPATH/ATHENA_BT_JOB/SYSTEMROOT) · DB 미전달 · OS 프로세스 · 타임아웃 kill(:71). 자식 `sandbox/__main__.py:76 main()` — `athena_bt` 별칭(:86), `guard.install()`(:103), `signals(bars, params)` 호출(:112), `signals` 없으면 오류(:110), `entry`/`exit` 열 강제(:115-117), `_coerce_signals`(:52)가 튜플/dict 관용.
- `guard.py` — `ALLOWED_TOP_LEVEL_IMPORTS = {pandas, numpy, math, statistics, datetime, athena_bt}` (:30), `BLOCKED_TOP_LEVEL_IMPORTS` (:42), `open()` jobdir 하위 제한(:87). "사고 방지 수준"(:3) — 위협 모델 = 사용자 검토 LLM 코드 (화면에 표시 필요).
- **stateless 1회성**: `run_strategy`가 매번 프로세스 기동·jobdir 삭제 (runner.py:107-118).

### 데이터·실행·러너
- `data.py:39 _SUPPORTED_PERIODS = ("day","week","month")` — 키움 REST 일/주/월 OHLCV뿐. `backfill(store, fetch_page, stk_cd, period, adjusted, base_dt, from_dt, ...)` (:227), `compute_plan` (:350) → `BackfillPlan{missing_days, estimated_pages, estimated_seconds}`, `kiwoom_fetch_page(client)` (:207). 캐시 sqlite `bt_candle`/`bt_coverage` (store.py:63,:75) — 단일 연속 구간.
- `runner.py` — `BacktestRunner.start_run(run_id, *, spec, df, overrides, initial_cash, extra_flags, source, python_exe, allowed_imports)` (:356); 코드 경로 `_run_code_signals()` (:82), `_align_signals(signals, df.index)` (:63 — 원본 봉에 reindex, 빈칸 False). 파라미터 우선순위 yaml default < 코드 PARAMS < override (:104). `Job{id, kind, status, progress, error}` (:192). `start_env()` (:275) 프로젝트 `.venv`.
- `engine.run_backtest` (engine.py:115)·`metrics.compute_metrics` (metrics.py:157) — 알람에는 불필요(체결·비용 없음).
- `deploy.evaluate_latest(spec, df, deployment, *, ..., signals=None)` (:113) — 코드 전략은 `spec=None` + 샌드박스 signals 전달(:134-139), **마지막 봉 한 개 판정**(`signals.iloc[-1]` :143). `Deployment{mode: observe|approve|auto, limits}` (:59), `bt_signal` 테이블(store.py:172).

### 노드·흐름·수리
- `flow.build_flow(source) -> FlowMap` (flow.py:207) — ast만. 4단계 `prepare/indicators/conditions/output` (`STAGE_LABELS` :25). `FlowNode{stage, title, first_line, last_line, source, produces, calls}` (:64). 불확실은 `unknown[]` 한국어. `:302` `returns_columns != {entry, exit}` 경고. `params_defaults(source)` (:143). `to_payload` (:316) = `POST /api/v1/backtest/flow`.
- `mapmodel.build_map(spec=, source=, result=, error=, version=)` (mapmodel.py:289) — 칸은 한국어 문장(`_OPERATOR_SENTENCES` :42), 사실은 마지막 실행값만(`_facts` :101), `_attach_error` (:260) 칸에 붙임, `BOUNDARY_AFTER_NOTE = "entry·exit 두 열만 받습니다"` (:38).
- `visual_schema.py`(1242줄) `VisualStrategyGraph`·`GraphIndex`·`compile_graph`·해시·`Diagnostic{severity, code, node_id, source_span}`. `visual_registry.py` 포트 4종(:33-37), 종류 `data.ohlcv/param/logic.and/logic.or/output.entry/output.exit` (:39-45), 지표 노드는 `indicators.list_all()`에서 파생.
- 수리 루프: `diagnose.diagnose(error_text, source, known_indicators, known_params) -> Diagnosis{title, detail, why, line, suggestion: Fix|None}` (diagnose.py:181) — 규칙표, 적용 안 함. `visual_repair.py` — `visual_question`/`visual_patch`, 편집 연산 allowlist 6종(:38), `base_graph_hash` 불일치 거절(:17), `applied:false`.

### 저장·등록·엔드포인트·MCP
- `projects/store.py` — `<projects_root>/registry.json` `{id,name,path,kind,created_at}`, 원자 교체, `resolve_in_project`(:140) 폴더 밖 금지, `.py`만, `venv_*`(:238-269), `SEED_STRATEGY_CODE`(:66).
- sqlite `store.py` SCHEMA_VERSION=1: `bt_candle bt_coverage bt_strategy bt_strategy_version bt_run bt_trade bt_equity bt_optimize bt_deployment bt_signal`.
- `api/backtest.py`(prefix `/api/v1/backtest`): `/presets`(:159) `/indicators`(:176) `POST /validate`(:200) `/data/coverage`(:231) `POST /data/plan`(:248) `POST /data/backfill`(:269, 사람 클릭 전용) `GET /jobs/{id}`(:285) `POST /runs`(:306 `{yaml, params?, source?, project_id?, allow_partial?}`) `/runs*`(:447-512) `POST /flow`(:529) `POST /map`(:539) `POST /codegen`(:581) `POST /diagnose`(:596) `/strategies*`(:620-978) `/optimize*`(:1037,:1060) `/deployments*`(:1185-1259) `/user-strategies`(:1401,:1446,:1476). `api/backtest_visual.py`(prefix `/api/v1/backtest/visual`): `/registry`(:78) `/validate`(:84) `/compile`(:117) `/question`(:150) `/patch`(:158) `/from-spec`(:182).
- MCP `athena_mcp/backtest_tools.py` 툴 `athena_backtest` (:39), `_ALLOWED_ACTIONS`(:41): list_presets list_indicators validate plan run status result list_strategies read_code **propose_code** flow diagnose map codegen optimize propose_spec navigate propose_optimize list_runs list_files read_file propose_file youtube_brief source_brief register_strategy visual_*(6). **backfill·activate·deploy 부재**(사람 클릭 전용). **정정(2026-09-03 재실측)**: `propose_code`는 프로젝트 폴더 `.py`로 가지 않는다 — `POST /api/v1/backtest/strategies/{id}/versions`로 가서 `bt_strategy_version`에 `origin=llm_draft`로 쌓인다(backtest_tools.py:975-982). `propose_file`은 HTTP를 타지 않고 **백테스트 캔버스**로 `file_draft` 카드를 보내 사람이 적용을 눌러야 디스크에 쓰인다(backtest_tools.py:841-846, app/main.js:2465-2470). 둘 다 백테스트 모드 착지점이라 **에이전트 모드 전용 코드 착지 경로가 따로 필요하다**. `action=run` 전달 키 `{yaml, params}`뿐(:100). 카드 봉투 `_canvas_envelope`(:668) `{delivered:"canvas", kind, payload, message, applied:false}`.

### 갭(코드 알람에 없는 것)
1. 연속 실행 계약 없음 — `signals(df,p)` 전체 히스토리 → 마지막 행 판정으로 재사용(=`evaluate_latest` 방식).
2. 분봉·실시간 데이터 없음(일봉까지). 틱 지표는 롤링 봉 버퍼 필요(2차).
3. 샌드박스 stateless — 주기마다 프로세스 기동 비용(30초 상한).
4. 출력 `entry/exit` 2열 고정 — `__main__.py:115`, `flow.py:302`, `mapmodel:38`, `_emit_outputs`, `visual_registry ENTRY/EXIT` 5곳. `exit=False`로 두고 `entry`=발화가 변경 0에 가깝다(권장).
5. 루틴 저장 모델이 코드를 못 담음 — `SourceSpec` transport `"code"` + 코드 위치 필드 + 판정 경로 + note 생성기 필요.
6. 쿨다운은 루틴 쪽에만 — 미니 백테스트에서 쿨다운 시뮬레이션 코드 없음.
7. 미니 백테스트 진입점 없음 — `_run_code_signals`+`_align_signals`까지 재사용, 발화 날짜·횟수만 세는 얇은 경로 신설.
8. 등록 게이트 — 사람 클릭(HTTP confirm)만.

## B. 루틴 실행기·앱 (확장 대상)

### `routines/`
- `models.py` — `Transport = ws|periodic|clock` (:19), `Mode = realtime-ws|periodic|scheduled` (:20), `SourceSpec(transport, value_type, ops, label, experimental)` (:28-36), `SOURCES` 6종(:42-51), `LEGACY_DISABLED_SOURCES`(:55-59, 유일한 periodic 소스는 죽어 있음 — **live periodic 없음**), `derive_mode()`(:101, `_TRANSPORT_TO_MODE` :94), `RoutineSpec{condition, symbol(6자리), cooldown_s, expires_at, note, id, status, created_at, approved_at, goal, briefing_model, briefing_effort}` (:146-162), `to_dict()`(:181, 파생 `mode` 포함), `ALLOWED_TRANSITIONS`(:66-73), `MAX_EXPIRY=30d`, `MIN/MAX_COOLDOWN_S=60/86400` (:228-230). **`Condition.value`는 리터럴만** — 코드 저장 자리 없음.
- `rules.py` — `validate_condition()`(:50) 유일한 Condition 팩토리, `_ALLOWED_CONDITION_KEYS={source,op,value,consecutive_ticks}`(:32) 그 밖 거부(:54-56), 값은 number|bool|string(:92), `consecutive_ticks>1` ws 전용(:100). `validate_draft()`(:108) symbol `^\d{6}$`·cooldown·expires_days 1..30·note≤200·goal·briefing_model·briefing_effort. **docstring :5-6 — exec/eval/compile 계열 금지, 소스 grep 테스트가 고정.**
- `runtime.py` — `RoutinesRuntime`(:34-46) store/ledger/engine/scheduler/events/read_marks/engagement/briefings/ws_client + refcount. `ensure_realtime_subscription`(:48, REG는 리미터 소모)·`release_realtime_subscription`(:58), `REALTIME_TR_IDS=("0B","1h")`(:30). **`can_activate(spec)`(:71)**: 레거시→blocked, realtime-ws→ws_client 필요, scheduled→OK, **그 밖→"이 source는 백엔드 실행 경로가 없어 활성화할 수 없다"(:85)**. `open_routines()`(:129) 부팅 시 재구독, 실패→`failed`+`routine-restore-failed`(:207-223). `_notify_factory`(:115) 큐 200.
- `triggers.py` — `TriggerEngine.evaluate(spec, observed, duration_ms=None) -> Verdict|None`(:89): 근접 슈미트(`NEAR_RATIO=0.9`/`NEAR_EXIT_RATIO=0.85` :21-25), consecutive 미달→suppressed, 쿨다운→suppressed(`쿨다운 N초 남음` :151), else fired(:158-168). `TriggerState`(:65) 프로세스 로컬. `reset_state`(:84). `duration_ms`(:98) 느린 관측용 칸 — 코드 실행 시간 자리. 산술 비교뿐(`_condition_met` :28) — bool 관측값 그대로 통과.
- `scheduler.py` — `RoutineScheduler`(:101), `start()`(:125-131) `_schedule_loop` 항상·`_archive_loop`·`_realtime_loop`(subscribe_ticks). `_realtime_loop`(:257) 단일 async 태스크 — **블로킹 subprocess 금지**. `_handle_verdict`(:223) near 진입/이탈 1회, fired→`_fire`(:144) payload `{type:"routine-fired", routine_id, symbol, source, mode, observed, threshold, note, goal, fired_at, briefing_model, briefing_effort}`(:154-168). `run_schedule_once()`(:293) 벽시계 KST, TriggerEngine 우회, `_last_fired_date`(:123). `_expire_pass`(:239). `record_scheduled_fire()`(:36). `adapt_real_message`(:70) → `(symbol, source, value)`; `_REAL_0B_FIELDS`(:28-33) 10→price.current 12→price.change_rate 228→trade.strength 851→volume.prev_day_ratio, 1h 9068→vi.triggered. **관측 표면 = 틱당 스칼라 5개, OHLCV·히스토리 없음.**
- `store.py` — 단일 JSON `RoutineStore(path)`(:31), 원자 저장(:58), `settings.routines_store_path`(config.py:110), 손상→`.corrupt`(:44-53), `transition()`(:104), `migrate_disabled_sources()`(:83).
- `ledger.py` — jsonl `record(verdict, routine_id, symbol, source, observed, threshold, reason, ts, duration_ms)`(:31), `Verdict = fired|near|suppressed`(:15), **원문·계좌·upstream 본문 금지(:3-4)**, `reason` 필수(:48). engagement.py·briefings.py(`routines_briefing_content_max_chars=4000` config:127)·read_marks.py, archive 90일(runtime.py:88-112).
- `guard_settings.py` — 전역 `GuardSettings(max_daily_nudges 0..10, quiet_hours, show_rationale, learn_from_dismissals, max_daily_briefings 0..50)`(:46-53). `GET /briefing-budget`(api/routines.py:222) 하루 브리핑 상한 — 코드 알람 브리핑도 상속.
- 설정: `config.py:109 routines_schedule_poll_interval_seconds: float = 20.0`, `:110 routines_store_path`.

### 키움 데이터
- 주기 REST 폴링 없음. `kiwoom/client.py:33 KiwoomClient` 범용 POST(`post_with_headers`:78, `post`:155, `is_ready`:60), `RateLimiter.acquire(api_id)`/`.headroom`(rate_limiter.py:37,42) 전 호출 게이트. TR 배선은 **`backend/athena_api/generated/`** — `generated/routes.py` `POST /api/v1/tr/{domain}/{tr_id}`, 명세는 `generated/registry.py`(`ka10001` 주식기본정보=현재가 :82, `ka10081` 주식일봉차트 :153). `ws_client.py:34 KiwoomWsClient` register/remove/subscribe_events/subscription_count.
- 샌드박스 자식은 자격증명·DB 없음 → **부모가 데이터를 받아 df로 넘긴다**(runner의 bars.csv 방식).

### REST·MCP
- `api/routines.py`(prefix `/api/v1/routines`, :14): `POST /draft`(:181) `GET ""`(:189 `{routines, last_error, fired_today}`) `GET /briefing-budget`(:222) `GET /source-catalog`(:247) `GET /{id}`(:265 `_detail_view` condition+source_spec) `POST /{id}/update`(:279; `_UPDATABLE_FIELDS`:147 note cooldown_s expires_days briefing_model briefing_effort, `_UPDATABLE_CONDITION_FIELDS`:148 op value consecutive_ticks; symbol·source 불변 :304-310; `validate_draft` 재검증 :327; `engine.reset_state` :337) `POST /{id}/confirm`(:341 `can_activate` 후 구독) `/pause`(:362) `/resume`(:374) `/cancel`(:394) `/catchup-fire`(:407) `/briefing-result`(:448) `GET /{id}/runs`(:504 `{runs, avg_duration_ms, opened_rate, replied_count}`) `/engagement`(:556) `/ack`(:578). `_runtime()` 503 unless `ATHENA_ROUTINES_ENABLED=true`(:98-105). 초안 body `{symbol, condition{source,op,value,consecutive_ticks?}, cooldown_s?, expires_days?, note?, goal?, briefing_model?, briefing_effort?}`.
- `athena_mcp/routine_tools.py` — `athena_routine`(:35) `_ALLOWED_ACTIONS=(draft,list,propose)`(:37), confirm/cancel 부재(테스트 고정 :8-10). `propose` 결과 `{control, routine_id, current, proposed, rationale, view, notice}`(:287-297), `_CONTROL_ACTIONS` 12종(:41-54). `dispatch()`(:208) 단일 루프백. 소스 enum은 `_INPUT_SCHEMA`(:85-101) 산문 — **"code" 소스는 여기 설명을 갱신해야 모델이 낸다.**
- WS 피드 `api/routines_ws.py:19 routine_notifications` — `feed-ready`, `near_snapshot()` 재생, 큐 펌프, 비활성 시 1013.

### 앱
- `app/lib/agent-canvas.js` — IIFE, `createAgentCanvas(deps)`(:151; container, fetchRoutines, fetchFiredToday, onNewTaskClick, pauseRoutine, resumeRoutine, fetchProfileSummary, onAddSuggestion, fetchAlerts, markAllAlertsRead, getWsConnected, fetchRuns, fetchAvgDuration, fetchEngagement, fetchNudgeGuard, onOpenGraph, onOpenInChat, onEditInChat). `VIEWS=[tasks, alerts, live, proactive]`(:110), `TABS=[all, active, paused]`(:95), `HISTORY_TABS=[runs, settings]`(:103). kind 3종: `toWatchItem`(:1282, active/paused && mode!=='scheduled', sub '실시간 감시' 또는 '주기 확인'), `toScheduleItem`(:1296), `toDraftItem`(:1310), `allItems()`(:1324). `statusRowIcon`(:140) mode 분기. `detailFieldsFor`(:1395)·`renderDetail`(:1422)·`openHistory`(:1063)·`renderHistoryRuns`(:909, `VERDICT_ICON` fired/near/suppressed :76-80)·`renderHistorySettings`(:1017). **새 mode는 `toWatchItem` else로 떨어져 '주기 확인'이 된다.**
- `app/canvas.js` — `createAgentCanvas` 배선(:2952), `fetchRoutines`/`fetchFiredToday` 각각 `athena:routines-list`(:2954-2967), `onNewTaskClick` → `seedChatInput('새 작업을 만들어줘 — ')`(:2969), pause/resume(:2980-2990), `onOpenInChat` → `AthenaNotify.selectRoom`(:3005). `createBacktestCanvas`(:2617), `backtestError()`(:2598), `window.AthenaBacktestCanvas`(:2929).
- `app/chat.js` — `refreshRoutineDrafts()`(:2921) `athena:routines-list` 폴링, `firstSeenAtById`(:2919). `renderApprovalCard(r)`(:2960) 필 작업 요약/초안, 제목 `r.note`, `draftDescriptionLine`(:2951), `approvalModeLine`(:2935) **3분기 — periodic '최대 폴링 주기만큼 지연' / scheduled '지정 요일·시각' / else '틱 즉시'**(:2940-2942). 칩(:3006-3044): **`미리보기 실행` disabled("백엔드에 대응 엔드포인트가 없다 … dry-run 개념 없음" :3014-3018)**, `바로 활성화` → `athena:routine-confirm`(activation_blocker면 disabled :3020-3033), `고칠 게 있어` seed(:3035). `renderMissedScheduleCard`(:2807), `RoutineTurn.buildTurnModel`(:2597), `lastFiredRoutine` → engagement replied(:881-888).
- `app/main.js` — `routineHttp`(:1192). IPC: routines-list(:1230) confirm(:1234) cancel(:1247) pause(:1251) resume(:1255) runs(:1260) ack(:1266) update(:1274) draft(:1280) detail(:1287) source-catalog(:1293) engagement(:1300) missed-confirm(:818) missed-skip(:850). `RoutineFeed` `${BACKEND_WS_BASE}/api/v1/ws/routines`(:634-636), `athena:routine-feed-status`(:644), `athena:routine-event` 셸·오브(:621,626), `routine-near` 배경(:583-586), `routine-fired`+scheduled → `runBriefingTurnWired`(:611-612,:704) → `/briefing-budget`(:714) → `/briefing-result`(:718-721). `backend-launcher.js` env 기본: `ATHENA_BRAIN_ENABLED`(:30-31) `ATHENA_ROUTINES_ENABLED='true'`(:33-34) `ATHENA_BACKTEST_ENABLED='true'`(:39-40) `ATHENA_BRAIN_INGEST_SCHEDULE_OWNER='external'`(:42-43).
- 백테스트 앱: `app/lib/backtest-canvas.js:97 DESIGN_TABS=[flow 지도, form 폼, code 코드·최후의 보루]`, `state.designTab='flow'`(:537), `loadMap()`(:1642,:2048), `NODE_BY_FIELD`/`MAP_NODE_ORDER`(:101,111), `changedNodeIds()` "방금 바뀜"(:1386-1402,:1478), 영수증+되돌리기(:1685). `backtest-code-editor.js`, `backtest-visual-editor.js`, `backtest-explain.js`. "이상해요" 문자열은 앱에 없음 — 수리 진입은 diagnostics→`/question`.
- 캔버스 공존: `app/lib/graph-mode/controller.js:325 applyVisibility()` 단일 소유, `SURFACE_BY_VIEW={summary, agent, plugin, backtest}`(:329-334), `elements.agent.hidden`/`elements.backtest.hidden`(:338-340). `sidebar-mode-nav.js` `createSidebarModeNav({items, badge, counts, onSelect})` 상태 비소유.
- Brain/graph: 루틴 발화는 brain에 미수집. UI 결합은 `GET /api/v1/brain/profile-summary`(canvas.js:2996-3001)와 "그래프 모드에서 근거 보기 →"(agent-canvas.js:82-93)뿐.

### 하드 제약 요약
1. `routines/` 안 exec/compile 금지(grep 테스트). 2. `Condition.value` 리터럴만·`_ALLOWED_CONDITION_KEYS` 외 거부 — 코드는 condition 밖에. 3. `can_activate` 미지 mode 거부(:85). 4. WS REG/REMOVE 리미터 소모·종목별 refcount — 틱 불필요 감시는 구독 잡지 말 것. 5. `_realtime_loop` 단일 태스크. 6. ledger 코드 원문·본문 금지, verdict 3종. 7. 쿨다운 60s–24h·만료 1–30d·consecutive_ticks 1–20(WS만); TriggerState·`_last_fired_date` 프로세스 로컬. 8. 모델은 draft/propose만. 9. 샌드박스 자식 자격증명·DB 없음 — 부모가 데이터 공급, REST는 리미터 소모. 10. `ATHENA_ROUTINES_ENABLED` 게이트 503.
