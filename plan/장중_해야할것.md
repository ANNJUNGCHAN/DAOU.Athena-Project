# 장중 — 해야 할 것 전체 리스트

> 최종 갱신: 2026-08-18 · 브랜치 `장중`
>
> **2026-08-18 전수 조사 스냅샷.** 병렬 판독 7건(인수인계 · 감시에이전트/캔버스 · 공통화면 ·
> 앱 · 백엔드/보안 · 코드 마커 · 누락검사)으로 `plan/` 17개 문서 · `app/` · `backend/` · `ui/` ·
> `GLOSSARY.md` · 코드 마커를 훑어 160건을 수집하고 중복을 접어 정리했다.
> 이 문서는 [`plan.md`](plan.md)를 대체하지 않는다 — plan.md §5가 현행 우선순위 정본이고,
> 여기는 그 밖에 흩어져 있던 항목까지 한 판에 모은 조사 결과다. 항목마다 근거 문서를 병기한다.

**큰 그림**: 네 갈래(MCP 게이트웨이 · 브레인 · 셀렉터 · Electron 두 창) 골격은 전부 돌고 있다.
남은 것은 ① 공통화면 G-로드맵 전체 ② 감시 에이전트 전체 ③ 카드/캔버스 설계 라운드 2
④ 보안 2건 마무리 ⑤ 브레인 후반부 ⑥ 문서 동기화 ⑦ 사용자만 풀 수 있는 차단 7건.

---

## A. 최우선 — 보안·결함

1. **프롬프트 인젝션 (HIGH) 잔여** — 2026-08-17에 응답 본문 라벨 마커가 붙어 부분 완화됐지만
   셋이 열려 있다: ⓐ `structuredContent` 채널은 라벨링 범위 밖 ⓑ 마커 위조·조기 종료 회피
   탐지 없음 ⓒ 내용 기반 지시문 탐지 미구현. (`backend/athena_mcp/SECURITY.md` §3.
   plan.md 액션 18은 이 부분완화 이전 기록이라 갱신 필요 — 아래 H-3.)
2. **응답 크기 상한 (MEDIUM) — 전송 계층 무방비** — 현재 상한은 post-parse뿐. SDK
   `stdout_reader()`가 개행까지 무제한 버퍼링해 상한 아래에서 메모리 고갈이 먼저 온다.
   `stdio_client` 로컬 포크가 필요. 앱(Electron) 쪽 스트림 버퍼 상한도 별도로 없다
   (`SECURITY.md` §4, `app/README.md` L262-266).
3. **ka10007 detail 9그룹 title에 TR ID 노출** — zero-tolerance 위반. `ref/`에 `title_ko`
   부여 → `generate_api.py` 재생성 → `--check` 녹색. 감사 문서가 **후속 1순위**로 지정
   (`kiwoom-common-screen-aits-coverage-audit.md` §8).
4. **G004 Electron 보안 전환** — 현재 `nodeIntegration:true` · `contextIsolation:false`.
   preload/contextBridge + screen-ID IPC로 전환. 공통화면 로드맵 소속이지만 보안 성격
   (`kiwoom-optimal-screen-selection-rendering-plan.md` §13 G004).
5. **KRX 키 과거 유출 이력** — 원격 히스토리에 실제 키가 남은 커밋 존재. 폐기·재발급 권고가
   미실행 — 사용자 판단 (`00-인수인계.md` §12).

## B. 공통화면 (갈래 B) — G-로드맵, G001만 완료

| 단계 | 내용 | 상태 |
|---|---|---|
| **G001.5** | 이질감 게이트 — 규칙 ≤10행 · override ≤40 · 자유 캔버스 ≤8 예산 + `fit_dissonance_check.py` + 산출물 JSON 3종. 코드 0건. 선행 [측정 필요] 다수(stratified 64개 목록, WS 정규식, width anchor 픽셀 보정) | 완전 미착수 |
| **G002** | ScreenDefinition/ScreenDocument 계약 — 301개 mapping 정규화, request leaf 4분류, `--check` stale 감지 | **다음 시작점** (handoff 명시) |
| **G003** | 공통 렌더러 6 primitive (facts/table/event/status/action/order_receipt) + 상태 7종 shell. 기존 코드에 loading/error 선례 없음. TableCard 연속조회 영역은 목업부터 미구현 | 대기 |
| **G004** | IPC · 보안 전환 · 백엔드 결선. **선결**: `apply_continuation_headers()` 호출부 0건 — 연속조회가 클라이언트에 미도달 | 대기 |
| **G005** | 301개 전수 DOM/IPC/field sentinel 검증 | 릴리스 게이트 |
| **G006** | Paper 아트보드 ↔ 실제 렌더러 대조 + 상태 variant 보완 | G002~4 이후 |
| **G007** | Runtime · Visual · Invariant · 독립 리뷰 closeout | 최종 |

- **주문 확인 모드** — 대화 창의 4번째 모드. 코드 0건인 유일한 모드. spec §3.5 · §6.2 ·
  아트보드 31 개정 동반 (`GLOSSARY.md` §1, aits 감사 §9 결정 ①).
- **ActionCard 정정/취소 변형 판정** — diff 표 vs 중첩 게이트, G001.5 시안 실물 비교 후 결정
  (감사 §9 결정 ②).
- **ka10173 동명 컨테이너 2개** — 규칙 사각지대, G001.5 착수 전 판정 목록 등록 (감사 §7 결함 2).
- **차트 렌즈 스펙 라운드 신설** — 토스증권 WTS 동등 목표(감사 §9 결정 ③). WTS 기능 실측 →
  렌즈 계약(저작 상태 · 저장 · 복원) 설계. 미착수.

## C. 카드·캔버스 (갈래 A)

- **W3 캔버스 어댑터** — upstream 출력 → 카드 데이터. `stream.py`가 여기서 첫 프로덕션
  호출자를 얻는다 (plan.md 액션 5).
- **타임라인 카드** — `app/canvas.js`에 분기 자체가 없다. 실배선이 안 닿는 유일한 카드
  (plan.md 액션 6). 가격축은 KRX 승인 전까지 pykrx 잠정. 키움 REST+MCP 데이터 이원 합성
  문제도 여기 걸린다 (`mcp-실행계획.md` §7 결정 4의 파급).
- **캔버스 설계 라운드 2** — 카드 12종 초안(`canvas-taxonomy.md`) → 시안. 카드 3개+ 동시 배치
  규칙, 실시간 19종 갱신의 시각 표현, 신규 카드 4종 Paper 시안 전부 미착수 (plan.md 액션 8).
- **자유 카드 W4 정식 설계** — 지금은 key/value 트리 최소 구현. 실왕복 3회 중 2회가 free
  폴백이라 긴급도가 실증됐다. AI 생성 코드 렌더 시 `<iframe sandbox>` + CSP + 네트워크 차단
  샌드박스 필수 — 리스크 '높음' (`mcp-실행계획.md` §4 W4).
- **MCP 공통 카드 봉투** — 설계·명세만 완료, 렌더러 미구현. 갈래 A "스칼라 카드 묶음" 본문이
  `canvas.py`에 없어 A2/A4가 갈 곳이 없다 (plan.md "MCP 공통 카드" 절).
- **`render_canvas` `oneOf` 스키마** — 카드별 `data` 스키마를 모델에 미리 알려 시행착오
  3회→1회로 (plan.md 액션 11).
- **실배선 QA 잔여** — stream · reader 실왕복 미확인(코드 배선 + 단위 테스트까지만), 멀티턴
  `--resume` 실왕복 미실측, 실배선 자동 검증 부재(fixture 고정 — plan.md 액션 14)
  (`app/README.md`).
- **카드 기능 세부** — 실시간 재갱신(폴링/WS) 없음, 접기 시 상태 초기화, 칩 강조 스크롤 복원
  미완 (`app/README.md` L609-614).

## D. 감시 에이전트 — 전부 미착수 (설계만)

근거는 전부 [`감시에이전트-실행계획.md`](감시에이전트-실행계획.md).

- **W2.5 파수꾼 엔진** — 시세 트리거 6종(급등락 · 거래량급증 · 호가불균형 · VI · 체결강도 ·
  52주 신고저). 스케줄러 자체가 코드 0건. **조기 착수 유리** — 원장이 쌓여야 W5 소급
  시뮬레이션이 성립한다 (§12).
- **원장(Ledger)** — 공통 테이블 카드의 인스턴스로 구현. 발화/근접/억제 3단 판정 +
  관측·알림 임계 분리 (§6).
- **W5 조사관 + 규칙 컴파일러** — 시세×뉴스 상관, 자연어→조건식 컴파일, 소급 시뮬레이션.
  W1 · W3 선행 필요 (§5-2, §7, §12).
- **알림 파이프라인** — 토스트→컨텍스트 복원 맵, 안전 고지 3건(종료 경고 · 뚜껑 경고 ·
  powerSaveBlocker), 알림 mutation 표면 판정 (§10, §14; aits 감사 §5.2 G4).
- **정량 게이트 임계값** — 선행성 · 고유성 · 유의성 (원장 축적 후 튜닝). 인과 적중 3번째
  피드백 컬럼도 미구현 (§5-2).
- **착수 전 확인 필요** (§14 미해결) — 시장 캘린더 자동 출처, `f_851` 실채움 여부,
  ka10171~74 서버사이드 검증, 해외지수·환율 MCP 소스(현재 0건 — "아침 브리핑" 원천 불가),
  `ui/round-1R/answer.md` 수치 정정(20종목 스캔 4초→실측 20초, WebSocket 예산).

## E. 투자 브레인

- **`fts` BM25 결선** — 로드는 되는데 검색이 여전히 `CONTAINS` 스캔 (plan.md 액션 12).
- **`IngestionCoordinator`** — `enqueue()`의 shutdown 가드(액션 17)를 먼저 닫고 lifespan
  결선(액션 16). 순서 주의 — 17이 16보다 먼저다.
- **조회 API 표면 0건** — `athena_api/api/`에 브레인 라우터 없음. G005가 정의한
  summary/search/neighborhood/timeline/evidence 전부 미착수 (`GLOSSARY.md`:275, 브레인 진행 문서 G005).
- **브레인 로드맵 G005~G009** — 배치 스케줄러, retrieval context, 프로덕션 UI, 패키징, 최종
  게이트. G004(LLM 추출)는 구현됐으나 독립 리뷰 승인 미완
  (`investment-brain-progress-2026-08-15.md` §8 — 2026-08-15 스냅샷이라 재확인 필요).

## F. 셀렉터·MCP·백엔드 기타

- **MCP 어댑터에 셀렉터 4툴 + `detail_group`** — `athena_mcp`에 selector 참조 0건.
  `backend/docs/LLM_API_SELECTION.md`의 어댑터 8요건 전부 미구현 (plan.md 액션 10).
- **셀렉터 실측 실패 2건** — realtime-0B-ko(의미론 갭), realtime-0H-en(tie-break 사각지대).
  해결 방향은 어휘집 추가가 아니라 tie-break 확장 (`LLM_API_SELECTION.md` Current progress).
- **plan_token 재사용 방지** — replay cache 없음, 3선택지 미결정 (`window-api-manual.md` §7-A).
- **409 clarification 스키마** — 사람이 읽을 제목 부재 (§7-B).
- **백엔드·Electron 동시 기동 스크립트** — bearer 토큰 이중 설정 해소 (§7-E).
- **계좌 표면 갭** — 연결 상태 3단계 필드 부재(`auth.py` TokenManager), 계좌별 주문 API
  게이트 부재(전역 bool 하나), backend 자격증명 OS 저장 계층 없음 (paper-specs AT-ST-001 · 007).
- **기타** — 원격(Streamable HTTP + OAuth) MCP 2단계, `--safe-mode` 실측, MCP 버전 혼재 반영,
  인코딩 스모크 테스트, 키움 TR MCP 노출 재판정, 신규 서버 캡처 프로세스 의무화, upstream
  진행 토큰(SDK 한계).

## G. 디자인 — 라운드 2 미결 질문 묶음

- **유리 적용 범위 모순** — round-1 "커맨드바 한정" vs round-1R "전면 유리", 명시적 해소
  없음. 카드 배경 결정이 여기 걸린다 (`kiwoom-common-screen-spec.md` §11 미해결 1).
- **리퀴드 글래스 모션 HTML 프로토타입** — 정지 시안으론 재료 판정 불가, "라운드 2 전 처리"
  명시 (`ui/round-1R/two-windows.md`, `ranking.md`).
- **굴절층/데이터층 분리** — soul.md §7 완화책 미구현, 확장 프레임 "부분통과"의 근본 원인
  후보 (plan.md 액션 7).
- 미결 질문: 오류 색 확정, 답변 2줄 규칙, 칩 없는 답변 종료 방식, 갱신 신선도 표시, 확장 중
  조작 경로, 자유 카드 빈도 임계, 두께 전이(0.82→0.97) 자연스러움, reduced-transparency
  전면 유리 레이아웃 성립, 타이틀바 문서·코드 정합, 밝은 배경 적응
  (`ui/round-1R/two-windows.md` · `answer.md` 미해결 절).
- **Paper 시각 검증** — 렌더러 무응답으로 23~40쪽 겹침·잘림 미확인, world 좌표 캐시 낡음
  (plan.md "Paper 화면설계서 정합" 한계).
- 스펙 잔여: AT-ST-004 후속 화면 미설계, AT-ST-005 위험 env 판별 규칙 미결, AT-SY-002
  자동전환 추정 결정, StatusCard 입력 영역, AT-CV-005 ID 규칙 (`plan/paper-specs/`).

## H. 문서 동기화 — plan.md가 코드보다 낡은 곳 (즉시 가능 · 저비용)

1. 액션 13 — Esc `killTree()` · 180초 타임아웃 · 선점 규칙으로 **대부분 해소됨**(응답 크기
   상한만 잔존). `app/README.md` L253-266.
2. 액션 15 — 실배선 카드 "2종" → 실제 4종(timeline만 남음). `app/README.md` L270-273.
3. 액션 18 — 프롬프트 인젝션 "아무 대응 없음" → 본문 라벨 부분완화 반영. `SECURITY.md` §3.
4. "MCP env 암호화 미룸" 절 — 실제로는 커밋 `be9c855`로 **닫힘** (README가 지적).
5. `backend/athena_mcp/README.md` 미구현 절 — CLI 왕복 · 본문 라벨 서술이 2026-08-17 이전 상태.
6. `kiwoom-common-template-fit-dissonance-plan.md` §9 · §10 — 폐기 확정된 주문 팝업 · 설정
   독립 창 설계에 폐기 표시 미반영.
7. 병합 커밋 약속 미이행 — `autoExpandCanvas/autoGrowChat` 화면 설정 미복구
   (`git show baa7e0e -- app/main.js`에 원본, 복구 시 `athena-prefs.json` 관례).
8. 소소한 정합 — 대화 3상태 명명(`GLOSSARY.md`:188-197), G002.5 이중 기록 재확인, 캔버스↔셀렉터
   4-tool 직접 배선 경로(`window-api-manual.md` §6)의 대체 여부 확인.

## I. 사용자만 풀 수 있는 것 (코드로 불가)

| # | 무엇 | 막히는 것 |
|---|---|---|
| 1 | **KRX 활용신청** (openapi.krx.co.kr 마이페이지) + 키 재발급 판단 | 타임라인 가격축 |
| 2 | **DART 키 발급** | 리더 카드 실데이터, truncate_at 실증, 당일접수 재확인 |
| 3 | **NAVER 키** | 스트림 카드 실데이터 |
| 4 | **키움 실계좌 키** | 인증 성공 경로 검증 |
| 5 | Gemini · Grok CLI 설치 머신 | CLI 4종 복구 |
| 6 | 용량 목표(LOC 상한) 결정 4 | G007 완료 조건 |
| 7 | CLI 로그인 왕복 수동 확인 (Codex는 probe 부작용 주의) | 인증 화면 완결 |

---

## 추천 착수 순서

문서들의 우선순위 표기(감사 §8 후속 액션, handoff §1, plan.md §5)를 종합한 순서다:

1. **A-1~3 보안·결함** — ka10007은 반나절감.
2. **문서 동기화 H** — 플랜 신뢰도 복구, 저비용.
3. **G002 착수** (handoff가 지정한 다음 시작점) + **W2.5 파수꾼 병렬** — 원장 축적이
   빠를수록 유리.
4. **타임라인 카드 · W3 어댑터.**
5. **설계 라운드 2** — G열 질문 일괄 처리.

I열의 키 발급 4건은 지금 신청해두면 뒤 단계 대기가 없어진다.
