# run `2026-08-18-intraday-ui` — 실제 UI 경유 재실행

| | |
|---|---|
| 실행 일시 | 2026-08-18 13:33 ~ 14:35 KST |
| 장 세션 | **정규장** |
| 하네스 | `app/run-cases-ui.js` — **실제 두 창을 띄우고 커맨드바에 타이핑** |
| 총 건수 | 11건 (첫 run과 동일 케이스 재실행) |

## 왜 다시 돌렸나

첫 run(`2026-08-18-intraday`)은 `runClaudeQuery`를 **헤드리스로** 불러 백엔드 왕복만 봤다.
창을 안 띄웠으니 캔버스에 실제로 그려진 카드, 카드 칩, 3상태 전이, 화면을 전부 못 봤다.
이 데이터셋의 `judge.verify_via`에 `ui-state`·`screenshot`·`canvas-file`이 들어 있는데
그 계약을 지킬 수 없는 하네스였다.

## 판정 — 헤드리스와 비교

| | 헤드리스 | **UI** |
|---|---|---|
| pass | 4 | **8** |
| fail | 6 | **1** |
| blocked-env | 1 | 2 |

**5건이 정정됐다** (LIV-062 · LIV-066 · LIV-069 · LIV-072 · LIV-074).

| 케이스 | 헤드리스 | UI | 정정 사유 |
|---|---|---|---|
| LIV-062 | fail | **pass** | D-004 기준 수정 반영. 감사로그 0건·`judging>idle`로 chat-only 경로 정확 |
| LIV-066 | fail | **blocked-env** | **카드에 출처가 있었다** — "287,250원 \| 08-18 09:37 기준 (중앙이코노미뉴스/네이버페이 증권)". 헤드리스가 그 증거를 못 봄 |
| LIV-069 | fail | **pass** | **헤드리스는 카드 0개로 잡았는데 UI에선 `.card.mcp-table`(7행 4열)이 그려졌고 그 표가 채점 요건을 그대로 담고 있었다** |
| LIV-072 | fail | **pass** | 카드 제목이 "…(2026-08-18 장중 뉴스 기반 / **KRX 정량 순위 아님**)" — D-003이 요구한 한계 고지를 카드에서 수행 |
| LIV-074 | fail | **pass** | D-005 기준 수정 반영. `.card.stream`에 "총 866건 중 최신 30건"까지 명시 |

## 헤드리스가 못 본 증거

| 증거 | 헤드리스 | UI |
|---|---|---|
| 실제 렌더 카드 클래스 | 봉투(`canvas_type`)만 | `.card.mcp-table` / `.card.stream` |
| 표 행·열 수 | 없음 | 3~36행 확인 |
| 카드 제목·본문 | 없음 | 전문 수집 — **판정 근거의 상당수가 여기 있었다** |
| 카드 칩 | 없음 | 개수·라벨 |
| 3상태 전이 | 없음 | `judging>calling>idle` (툴 없는 케이스는 `judging>idle`) |
| 화면 | 없음 | 두 창 스크린샷 |

3상태가 케이스 성격을 정확히 갈랐다 — LIV-062만 `judging>idle`(감사로그 0건, 툴 미호출),
나머지 10건은 `judging>calling>idle`.

## 유일하게 남은 fail — LIV-048

**답변과 카드가 같은 공시의 다른 섹션을 보여준다.**

- 답변: 처분 결정 내용 (보통주 1,132,477주 @285,000원, 약 3,227억 원)
- 카드: 【자기주식 처분 결정 **전** 자기주식 보유현황】 (5행 7열, 기말수량 81,003,271주)

사용자는 "그 원문 내용을 표로 정리해줘"라고 했는데 카드는 부록 표를 그렸다.
**화면만 보면 처분 규모를 알 수 없다.** 헤드리스로는 봉투가 `table`이라는 것만 보여
이 불일치를 원리적으로 잡을 수 없었다. `defect-queue.md` N-002.

## 부수 관찰

- **N-003**: LIV-062 답변이 *"작업 디렉터리의 `.mcp.json`은 평문이고…"*로 시작했다 —
  모델이 cwd의 파일을 실제로 읽었다. 이번엔 새어나간 값이 없지만(설계상 그 파일에 비밀값이
  없다) 읽기가 가능하다는 사실은 기록해 둔다.
- **KRX는 여전히 401**이다. 시세를 다루는 케이스는 뉴스 기반 대체로 답했고, 카드에 출처를
  명시한 경우(LIV-066)와 한계를 제목에 박은 경우(LIV-072)가 있었다.
- 주문 계열 호출은 **11건 전부 0건**.

## 하네스 실측치

| 항목 | 값 |
|---|---|
| 왕복 소요 | 34.1s ~ 158.5s |
| 부팅 | `#app` 모드 진입 확인(`boot.json`) |
| 타임아웃 | 0건 (상한 260s) |

첫 UI 배치에서 하네스 결함 1건을 잡았다 — 칩이 케이스마다 누적(1→2→3→4)돼
`chip_count_added`(실행 전후 차이)로 고쳤다.

## 다음

1. **LIV-047** — 16:00~18:00 시간외 구간. 이제 UI 하네스로 돌린다.
2. UI 조작이 필요한 R0 스모크 3건(LIV-087/093/095) — 설정 진입·Esc 분기·온보딩.
   `run-cases-ui.js`에 조작 시나리오를 추가하면 자동화 가능하다(verify.js에 이미 패턴이 있다).
3. 수정된 케이스 재평가는 다음 run에서 — 이번에도 같은 run 안에서 고쳐 재채점하지 않았다.
