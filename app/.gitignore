node_modules/

# 실행 로그. 매 실행마다 타임스탬프가 바뀌어 병합할 때마다 충돌한다.
# 검증의 안정 증거는 captures/VERIFY-REPORT.json과 캡처 PNG다.
captures/*.log

# verify.js가 쓰는 검증 전용 Electron 프로필(userData). 매 실행마다 새로 만든다.
# 검증이 이 머신의 개인 상태(계좌·온보딩·비밀값)에 의존하지 않게 하는 격리 장치다.
.verify-profile/
.capture-wide15-profile/
.probe-chart-profile/
