# archive — 낡은 아키텍처의 캡처

여기 있는 캡처는 **2026-08-16 구조 변경 이전**의 증거다: 계좌·MCP 카드가 아직
캔버스 창에 그려지던 시절(D7-01~03, 카드별 닫기 검증)이라, 현행 아키텍처
(계좌·MCP 카드는 대화 창 설정 모드 소속 — `styles/settings-cards.css` 상단 주석)와
맞지 않는다.

지우지 않고 옮겨둔 이유: D7(카드별 닫기) 기능 자체는 지금도 유효하고, 이 캡처가
그 구현 당시의 검증 증거이기 때문이다. **현행 화면의 증거로 인용하지 마라** —
현행 증거는 `../*.png`(verify가 재생성)와 `../VERIFY-REPORT.json`이다.

2026-08-17 디자인 리뷰 [LOW] 지적으로 분리했다. `spike/captures/` 불변 규칙
(CLAUDE.md §3)은 spike 쪽 이야기라 이 폴더에는 적용되지 않는다.
