# Paper → 실앱 적용 대응표

기준 시각: 2026-08-29 KST<br>
Paper 파일: `Athena — 코드 기반 화면 (chat · canvas · settings · orb)`<br>
현재 범위: **96 artboards = 화면 60 + 부팅 화면 상세 5 + 카드 31**<br>
카드 작업 범위: **인증 2종을 제외한 299 operations 전수**

## 완료 판정

- `적용`: 별도 라우트 또는 실제 상태/컴포넌트로 실앱에서 접근 가능하다.
- `공통 계약`: 독립 화면이 아니라 여러 화면이 공유하는 시각·상호작용 계약으로 적용됐다.
- `참고`: Paper가 명시한 이전 셸 참고본이며 중복 라우트를 만들지 않고 현재 셸에 흡수했다.
- `카탈로그`: 독립 앱 화면이 아니라 데이터 응답을 실제 카드로 변환하는 렌더 계약이다.

실앱의 모드 계약은 대화/이력 셸을 유지하고 중앙 캔버스만 `대화 · 그래프 · 에이전트 · 플러그인`으로 전환한다. 인증 2종은 카드로 위장하지 않고 인증 상태 화면에 남긴다.

## 화면 페이지 — 60/60

| Paper artboard | 실앱 소유 표면 | 판정 |
| --- | --- | --- |
| 01 · 온보딩 1/3 — 부팅 4단계 | `shell.html`, `boot.css`, `boot.js` | 적용 |
| 02 · 온보딩 2/3 — CLI 연결 | `onboarding.js`, 셸 온보딩 오버레이 | 적용 |
| 03 · 온보딩 3/3 — 계좌 연결 | `onboarding.js`, 계좌 연결 단계 | 적용 |
| 04 · 셸 창 (기본) | `shell.html`, `shell.css`의 268px 이력 + 캔버스 + 400px 대화 | 적용 |
| 05 · 셸 — 답변 모드 | 대화 모드 빈/답변 상태, 입력·모드 칩 유지 | 적용 |
| 06 · 셸 — 그래프 모드 · 요약 | 그래프 모드 요약 캔버스 | 적용 |
| 07 · 셸 — 그래프 모드 · 행 선택 | 그래프 요약 선택 상태 | 적용 |
| 08 · 셸 — 알림 파생 방 | 사이드바 알림 방과 읽음 상태 | 적용 |
| 09 · 대화 상태 — 모델 팝오버 · 루틴 승인 | `chat.js`, 모델 팝오버와 승인 카드 | 적용 |
| 10 · 캔버스 카드 — reader · facts · compound · free | `canvas.js` 카드 렌더러 | 적용 |
| 11 · 캔버스 카드 — 보호 워크플로 | event/action/status 카드와 승인 상태 | 적용 |
| 12 · 캔버스 카드 — 주문 티켓 | 셸 내부 `#order` 티켓, 실행 게이트 | 적용 |
| 12d · 공통 카드 v3 16종 · 206/206 | 최신 299/299 카드 계약으로 상향 적용 | 공통 계약 |
| 13 · 차트 카드 세부 | 주기·지표·드로잉·매물대 패널 | 적용 |
| 14 · 그래프 뷰 — 기본 | 7개 군집 지도 | 적용 |
| 15 · 그래프 뷰 — 군집 펼침 + 노드 선택 | 그래프 선택/상세 상태 | 적용 |
| 16 · 계정 메뉴 | 사이드바 하단 계정 메뉴 | 적용 |
| 17 · 설정 모드 — 화면 카드 (Paper 화면 13 · 설정 — 셸 오버레이) | 셸 설정 오버레이 — 좌측 nav 4종(화면·계좌·모델·그래프). `플러그인` 항목은 2026-09-03 제거 | 적용 |
| 18 · 설정 — 계좌 카드 | 설정 계좌 패널 | 적용 |
| 19 · 설정 — 플러그인 카드 (Paper 화면 31 · 스니펫 등록·감사 로그) | 폐기 — 2026-09-03 플러그인 모드 관리 뷰(플러그인 02)로 흡수. 보드는 폐기 각주만 남긴다 | 이동 |
| 20 · 설정 — 모델 카드 | Claude/Codex 모델 패널 | 적용 |
| 21 · 설정 — 성향・이력 카드 | 설정 성향/이력 패널 | 적용 |
| 22 · 설정 — 그래프 | 수집·노출 설정, 성향은 캔버스 소유 | 적용 |
| 23a · 계좌 등록 — 인증 확인 중 | 계좌 연결 pending 상태 | 적용 |
| 23b · 계좌 등록 — 인증 실패 | 계좌 연결 failed 상태 | 적용 |
| 23c · 계좌 등록 — 확인 완료 | 계좌 연결 ready 상태 | 적용 |
| 24 · 주문 API 활성화 게이트 | 주문 API 승인 시트 | 적용 |
| 25 · 플러그인 설치 · 승인 | 별도 **플러그인** 페이지 02·05로 이동 | 이동 |
| 26 · 플러그인 기능 허용 | 별도 **플러그인** 페이지 01로 이동 | 이동 |
| 27 · 인증 — 토큰 상태 | 인증 ready 상태 카드/화면 | 적용 |
| 28 · 인증 — 토큰 4상태 | needed · ready · refreshing · expired | 적용 |
| 29 · 인증 — 계좌 전환 | 계좌 전환 상태 | 적용 |
| 30 · 오브 — 상황별 표현 매핑 | 오브 상태/크기 매핑 | 적용 |
| 31 · 오브 표정 10종 | 공식 눈 모양 상태 | 적용 |
| 32 · 오브 표현 엔진 | 시선 9방향·시간 루프 | 적용 |
| 33 · 오브 대화 | 3단계 크기와 콘텐츠 전개 | 적용 |
| 34 · 오브 표시 모드 2종 | 셸 숨김/표시 모드 | 적용 |
| 35 · 접근성 폴백 | 투명도 감소·모션 감소 | 적용 |
| 36 · 주문 게이트 | 확인·보류·실패 분리 | 적용 |
| 37 · 이전 셸 v2 참고 — 대화모드 | 현재 04/05 셸 계약에 흡수 | 참고 |
| 38 · 이전 셸 v2 참고 — 그래프모드 | 현재 06/07/14/15에 흡수 | 참고 |
| 39 · 이전 셸 v2 참고 — 에이전트모드 | 현재 40–43에 흡수 | 참고 |
| 40 · 에이전트 — 알람 센터 · 라이브 관제 | 에이전트 알람/라이브 캔버스 | 적용 |
| 41 · 에이전트 — 실행 이력 · 결과 | 작업 드릴인과 실행 이력 | 적용 |
| 42 · 에이전트 — 프로액티브 | 프로액티브 제안 캔버스 | 적용 |
| 43 · 에이전트 — 새 작업은 채팅에서 | 채팅 입력 + 캔버스 초안 승인 | 적용 |
| 44 · 모드 전환 UX | 4개 모드 배타성, 채팅 지속 | 적용 |
| 45 · 입력 스트립 — 키우미 | 키우미 페이지 08로 승격 — 5모드 얼굴 전체 | 적용 |
| 46 · 빈 캔버스 — 모드별 | 대화/그래프 빈 상태 | 적용 |
| 47 · 플러그인 — 허브·설치 | 별도 **플러그인** 페이지 03으로 이동 | 이동 |
| 48 · 플러그인 — 관리·마켓플레이스 | 별도 **플러그인** 페이지 04로 이동 | 이동 |
| 49 · 키우미 메뉴 | 키우미 페이지 06·07로 승격 — 380px 굴절형, 8개 SVG 액션 | 적용 |
| 50 · 대화 턴 — 복원 실패 | 복원 실패 턴 상태 | 적용 |
| 51 · 캔버스 카드 공통 규격 | 캔버스 카드 공통 타이포·간격·재질 | 공통 계약 |
| 52 · 공통 셸 v2 규격 | 지속 이력/채팅 + 중앙 모드 캔버스 | 공통 계약 |
| 53 · Windows 반응형 창 · Snap | 1280+ 3열, 700–1279 2열, 330–699 레일 | 적용 |
| 54 · 프로젝트·최근·새 채팅 | 공유 identity, 첫 입력 전 빈 행 금지, hover/menu | 적용 |
| 55 · 리퀴드 글래스 배경 | B 블루 포그 배경 | 공통 계약 |
| 56 · 접근성 상태 색 비교 | 성공·경고 색과 비색상 단서 | 공통 계약 |
| 57 · 리퀴드 글래스 굴절 재질 | 환경광·흡수 그라디언트·정반사·양면 엣지 | 공통 계약 |
| BT-01 · 백테스트 — 설계 (폼) (백테스트 페이지) | 다섯 번째 모드 · 대상·기간/지표/조건/리스크·비용 폼 + 접히지 않는 체결 가정 | 적용 |
| BT-02 · 백테스트 — 설계 (코드) (백테스트 페이지) | 파이썬 편집기 · LLM 코드 초안은 diff+사람 적용 · 샌드박스 경계 명시 | 적용 |
| BT-03 · 백테스트 — 결과 (백테스트 페이지) | 지표 타일 6장 · 자산곡선(전략 vs 매수보유) · 체결 표 · 가정 상시 표기 | 적용 |
| BT-04 · 백테스트 — 데이터 수집 승인 (백테스트 페이지) | 커버리지 바 · 호출 수·예상 소요 선표시 · 사람 클릭 전용 수집 · 보유 구간만 실행 | 적용 |
| BT-05 · 백테스트 — 이력·비교 (백테스트 페이지) | 실행-버전 결속 · 2건 선택 비교 · 파라미터/코드 diff · 곡선 겹쳐보기 | 적용 |
| BT-06 · 백테스트 — 최적화 (백테스트 페이지) | 그리드/랜덤 · Sharpe 히트맵 · 외딴 봉우리 과최적화 경고 | 적용 |
| BT-07 · 백테스트 — 전략 배포 · 실전 적용 (백테스트 페이지) | 검증한 버전 결속 · 실행 모드 3종 · 사람이 정하는 한도 · 신호→주문 단계 · 실전 vs 백테스트 괴리 | 신호·한도 판정 적용 · 실주문 연결은 주문 게이트 몫 |
| BT-08 · 백테스트 — 코드 플로우 지도 (백테스트 페이지) | 코드 4덩어리 ↔ 지도 칸 · 앱/내 코드 경계 두 줄 · 단일 파일 실행 | 적용 |
| BT-09 · 백테스트 — 오류 진단 · 자동 수정 승인 (백테스트 페이지) | 확정 후 4단계 · 어디서·왜·어떻게 · 사람이 눌러야 적용 | 적용 |
| BT-10 · 백테스트 — 전략 고르기 · 실패·비활성 상태 (백테스트 페이지) | 프리셋 10종 진입로 · 실패/비활성/진행 유지 3상태 · 막다른 길 금지 | 적용 |
| BT-11 · 백테스트 — 시각 전략 설계 · 편집 가능 (백테스트 페이지) | 팔레트·노드/포트/엣지 캔버스·검사기·검증 요약 바 · 서버 검증 가능 상태 · 대화가 만든 비활성 시각 초안 | 구현 중 (ralph US-006·007) |
| BT-12 · 백테스트 — 시각 전략 검증 · 연결 오류 (백테스트 페이지) | 오류 노드·포트·엣지·검사기 같은 code · 실행 차단 · 대화는 한 번에 질문 하나 | 구현 중 (US-002·007·009) |
| BT-13 · 백테스트 — 오류 노드에서 코드로 · 줄 연결 (백테스트 페이지) | 더블클릭·Enter → 코드 span · 연결 노드 리본 · authoritative/preview 구분 · 그래프+코드 패치 diff | 구현 중 (US-003·008·009) |
| BT-14 · 백테스트 — 그래프·코드 동기화 완료 · 실행 전 (백테스트 페이지) | 적용 receipt → 재검증·컴파일 → origin=visual·비활성 v+1 · 활성화·실행 미자동 | 구현 중 (US-004·009·010) |
| BT-15 · 백테스트 — 설계 (흐름 지도) · 대화로 고치는 1급 표면 (백테스트 페이지) | 지도 탭 첫 표면 · 칸 ①~④ 사람 말 · 지난 실행 실제 값 · 지도 반영 카드 · 코드 서랍(최후의 보루) | 적용 (4b0df46) |
| BT-16 · 백테스트 — 지도 실행 → 멈춤 → 대화로 고침 (백테스트 페이지) | 오류가 칸에 · 진단 카드 지도 아래 · [적용하고 다시 실행]/[지도만 고치기] | 적용 (4b0df46) · 프로브 N05 검증 중 |
| BT-17 · 백테스트 — 출처에서 지도로 · 만드는 중 (백테스트 페이지) | URL → 5단계 진행 띠 · 칸이 하나씩 채워짐 · 출처 요약 카드 | 설계 완료 · 구현 전 |
| BT-18 · 백테스트 — 흐름 지도 강화 명세 (백테스트 페이지) | A 칸 6종 · B 동사 9개 · C 버전 · D 결과가 칸에 · E 지도↔코드 · F 로딩 · G 폼·프리셋 · H 실매매 | 규칙 보드 — A·B·C·D·E 적용, F·H 부분 |

백테스트 보드 10장은 별도 **백테스트** 페이지에 산다(2026-08-31 사용자 지시 — 백테스트 관련 디자인은 백테스트 페이지로). 화면 페이지 쪽 개정: 45(모드 전환 UX — 5모드 카드 추가 완료) · 47(빈 작업공간 — 백테스트 빈 상태 추가 완료) · 46(키우미 — 백테스트 얼굴은 각주만, 얼굴 원화는 후속).

2026-09-01 전수 파리티: BT-07~10 4장을 새로 그리고 실앱을 Paper 60개 항목 전부에 맞췄다. 감사 표와 구현 결과는 `docs/architecture/backtest-parity-audit.md`에 있다.

2026-09-03 위계 개정: 사용자 지시 "코드는 최후의 보루 — 대화로 코드 플로우 지도를 고치고, 지도 뒤에 코드가 있다"에 따라 흐름 지도 보드 4장(15~18, y=4760 행)을 그렸고 4b0df46으로 앱에 올렸다(지도 탭이 첫 표면, mapmodel/codegen 백엔드 aab199f). 같은 날 Codex 세션이 시각 전략 설계(노드·포트 편집기) 보드 4장(11~14, y=3560 행)과 docs/research 두 문서(오픈소스 벤치마크·왕복 구현 평가)를 냈다 — 지도 탭이 곧 편집 가능한 VisualStrategyGraph가 되는 방향으로 ralph가 P0~P3을 구현 중이다. 02·08·09는 초기 안으로 표시했다.

## 플러그인 페이지 — 9/9

기준 시각: 2026-09-03 KST. 플러그인 보드는 백테스트와 같은 방식으로 별도 **플러그인** 페이지에 살며, 2026-09-01 전수 검사에서 05·06을, 2026-09-03 기조 정렬에서 07·08·09를 더해 9장이 됐다. 아트보드 치수는 01~05·07~09가 1680×986, **06은 1680×830**(6칸 = 3행 × 2열)이다.

**모든 변경은 승인 카드 하나를 지난다(2026-09-03).** 화면의 버튼은 아무것도 실행하지 않는다 — GUI 진입 6종과 모델 제안이 같은 봉투를 만들어 `#pluginCanvas` 안의 승인 카드로 합류하고(`app/lib/plugin-canvas.js` · `app/canvas.js`), 사람의 [승인] 하나만 실행 경로다(`app/lib/main/plugin-proposal-registry.js`). 그 아래는 그대로다: 등록·승인·probe·도구 허용·승인 철회·삭제가 전부 `backend/athena_mcp` CLI를 실제로 spawn한다(`app/lib/main/mcp-cli.js` → `app/main.js`의 `athena:mcp-*` 핸들러). 추천 목록은 앱 내장 카탈로그(`app/lib/plugin-catalog.js`)이며 5종 모두 API 키가 필요 없고, 이 저장소에서 제안→승인→등록→probe까지 실제로 돌려 확인했다.

| Paper artboard | 실앱 소유 표면 | 판정 |
| --- | --- | --- |
| 01 · 플러그인 — 기능 허용 (AT-ST-006) | `plugin-canvas.js` 권한 상세 화면 · 배지 `설치됨·기능 N·허용 N·연결 상태` · 하단 내장 API 경계 문구 · [선택 저장]은 권한 초안을 승인 카드로 넘긴다 | 적용 |
| 02 · 플러그인 — 직접 등록·감사 로그 (관리 뷰) | `plugin-canvas.js` 관리 뷰 — `직접 등록`(허브 `+ 서버 추가` 시트 재사용) + `감사 로그` 구역 | 적용 |
| 03 · 플러그인 — 허브·설치 | `plugin-canvas.js` 허브 · `설치됨`/`추천` · `권한`/`설치` · `+ 서버 추가` · 채팅 헤더 `아테나 · 플러그인 대화` / `설치와 권한을 여기서 정합니다` | 적용 |
| 04 · 플러그인 — 관리·마켓플레이스 | `plugin-canvas.js` 관리 · 켜기=승인 / 끄기=승인 철회 / 삭제=레지스트리 제거 · 낙관적 토글 없음(`승인 후 반영됩니다`) | 적용 |
| 05 · 플러그인 — 설치 승인 | 카탈로그 설치 시트(제공·용도·요청 기능·실행 명령·설치 위치·거부/승인) → 같은 제목 `웹 문서 읽기 설치`의 승인 카드로 합류. 시트는 승인 표면이 아니라 카드로 가는 문이다 | 적용 |
| 06 · 플러그인 — 상태 모음 (1680×830) | 빈 허브 2종 · probe 실패 배너와 재시도 · 토글 실패 되돌림 · 재시작 안내 + 승인 카드 `제안 대기`·`거부됨` 2상태 | 적용 |
| 07 · 플러그인 대화 — 제안 턴 5동작 | `chat.js` `renderPluginProposalTurn`(모델 제안만 채팅 턴) + `plugin-canvas.js` 승인 카드 — 설치·허용/철회·켜기끄기·삭제·직접 등록 5동작이 같은 카드 한 종류로 그려진다 | 적용 |
| 08 · 플러그인 대화 — 결과 턴 | `chat.js` `athena:plugin-result` 구독 — 성공(등록·연결 확인·기능 N개·`다음 실행부터 반영됩니다`) / 거부(`그대로 뒀습니다`) / 실패(+`다시 시도` 칩) / 만료. 모드 밖 폐기 한 줄은 `플러그인 모드에서 다시 요청합니다` | 적용 |
| 09 · 플러그인 창 복원 | `canvas.js` `AthenaPluginCanvas.setView`가 모드 재진입마다 `athena:plugin-pending`으로 미해결 봉투를 되살린다 · 권한 초안은 `plugin-canvas.js`의 `permissionDraft`가 붙들고 · 미전송 입력은 오늘 입력줄이 제출 시에만 비워지므로(`app/chat.js`의 `$input.value = ''`) 모드 이탈·재진입에서 그대로 남는다 | 적용 |

### 내장 카탈로그 5종 (2026-09-01 실측)

| id | 이름 | 실행 명령 | probe로 확인한 도구 |
| --- | --- | --- | ---: |
| `fetch` | 웹 문서 읽기 | `uvx mcp-server-fetch` | 1 |
| `time` | 시간·시간대 | `uvx mcp-server-time` | 2 |
| `sequential-thinking` | 단계적 사고 | `npx -y @modelcontextprotocol/server-sequential-thinking` | 1 |
| `memory` | 지식 그래프 메모리 | `npx -y @modelcontextprotocol/server-memory` | 9 |
| `korea-stock` | 한국 주식 시세 | `npx -y @drfirst/korea-stock-mcp` | 6 |

설정에는 플러그인 표면이 없다(2026-09-03 — 그 전까지 설정 좌측 `플러그인` 항목이 스니펫 직접 등록과 감사 로그를 맡았다). 플러그인 모드 하나가 허브·추천 설치·기능 허용·직접 등록·감사 로그를 전부 맡는다 — 같은 레지스트리를 두 화면에서 부르지 않는다.

## 부팅 화면 상세 페이지 — 5/5

| Paper artboard | 실앱 상태 | 판정 |
| --- | --- | --- |
| 01 · READY · 0–240ms | 회색 ATHENA + 핑크 커서 | 적용 |
| 02 · TYPE A→AT · 240–560ms | 파란 접두사 타이핑 | 적용 |
| 03 · TYPE ATH→ATHE · 560–880ms | 오버레이 타이핑 진행 | 적용 |
| 04 · COMPLETE · 880–1440ms | ATHENA 완성 + 유지 | 적용 |
| 05 · DIRECT SHELL EXPAND · 1440–1920ms | 중간 가짜 창 없이 실제 셸 확장 | 적용 |

`prefers-reduced-motion`에서는 같은 단일 완료 경로를 쓰되 완성 정적 프레임으로 축약한다.

## 카드 페이지 — 31/31

### 렌더 패밀리 카탈로그 — 13/13

| Paper artboard | 실앱 계약 | 판정 |
| --- | --- | --- |
| 00 · 카드 카탈로그 — 299/299 | 전체 operation → 카드 봉투 | 카탈로그 |
| 01 · F1 Facts — 93 | facts 카드 | 카탈로그 |
| 02 · F2 Dense Facts — 21 | dense facts 카드 | 카탈로그 |
| 03 · T1 Compact Table — 32 | compact table | 카탈로그 |
| 04 · T2 Standard Table — 60 | standard table | 카탈로그 |
| 05 · T3 Wide Table — 12 | wide table | 카탈로그 |
| 06 · T4 Compound Table — 10 | compound table | 카탈로그 |
| 07 · C1 Chart — 19 | AITS chart | 카탈로그 |
| 08 · C2 Compound — 17 | chart/summary compound | 카탈로그 |
| 09 · E1 Realtime Event — 21 | realtime event | 카탈로그 |
| 10 · E2 Condition Event — 1 | condition event | 카탈로그 |
| 11 · E3 Stop Event — 1 | stop event | 카탈로그 |
| 12 · A1 Guarded Order — 12 | guarded order action | 카탈로그 |

위 13개 보드의 합은 299이며, 실제 렌더러의 `facts · table · compound · chart · event · action · status` 계열로 수렴한다.

### AITS × Toss v3 공통 계약과 16개 창 — 18/18

| Paper artboard | operation 수 | 고정 실앱 카드 제목 | 판정 |
| --- | ---: | --- | --- |
| 13 · 공통 카드 v3 기준안 | — | 공통 창 단위 계약 | 공통 계약 |
| 14 · 카드 v3 카탈로그 | 299 | 아래 16종 전수 | 카탈로그 |
| 15 · 차트 | 21 | 차트 | 적용 |
| 16 · 호가 | 28 | 호가 | 적용 |
| 17 · 주문 | 9 | 주문 | 적용 |
| 18 · 시세 | 23 | 시세 | 적용 |
| 19 · 종목발굴 | 66 | 종목발굴 | 적용 |
| 20 · 관심종목 | 3 | 관심종목 | 적용 |
| 21 · 종목정보 | 25 | 종목정보 | 적용 |
| 22 · 거래원 | 17 | 거래원 | 적용 |
| 23 · 계좌 | 50 | 계좌 | 적용 |
| 24 · 보유주식 | 7 | 보유주식 | 적용 |
| 25 · 주문내역 | 11 | 주문내역 | 적용 |
| 26 · 수급 | 10 | 수급 | 적용 |
| 27 · 프로그램매매 | 10 | 프로그램매매 | 적용 |
| 28 · 신용거래 | 14 | 신용거래 | 적용 |
| 29 · 대차거래 | 4 | 대차거래 | 적용 |
| 30 · 공매도 | 1 | 공매도 | 적용 |

16개 창의 합은 정확히 299다. `backend/athena_api/canvas_transform.py`가 operation의 도메인·화면 접근성 라벨·검증된 예외만으로 고정 제목을 결정하며, 미분류를 허용하지 않는 전수 테스트가 있다.

### 카드 스트립 버튼 상태 전수 — 2026-09-01

사용자 지시: 카드 우상단 스트립의 **버튼을 눌렀을 때의 모든 경우의 수**를 카드 페이지 보드로 담는다. 탭 상태 보드는 `R0n-T*` 접미사를 쓴다(기존 `R02-T2·T3`, `R03-T1·T2` 규약). 이번에 6장을 신설했다.

| 카드 | 스트립 버튼 | 커버 |
| --- | --- | --- |
| CC-03 차트 | 현재시세 · 차트 · 기업정보 · ETF · ELW · 금현물 · **순위** | **7/7 완료** — T1 현재시세·T2 기업정보·T3 금현물 신설, 차트=R01 · ETF=R07 · ELW=R08 · 순위=R01-T4·T5·T6 (2026-09-01 4차 신설) |
| CC-05 수급 | 투자자별 · 거래원 · 프로그램 · 신용·대차 · 종목 동향 · **순위** | **6/6 완료** — T3 프로그램·T4 신용·대차·T5 종목 동향 신설 · 순위=R03-T6 (2026-09-01 4차 신설) |
| CC-04 호가 | 정규장 · 시간외 · 금현물 ‖ 5단 · 10단 | **5/5 완료** — R02-T4 정규장 5단 신설 (10단=R02 · 시간외=T2 · 금현물=T3) |
| CC-01 계좌 | 자산 종합 · 보유종목 · 예수금·결제 · 손익·성과 · 주문·체결 · 증거금·담보 · 금현물 | **7/7 완료** — R10-T1 보유종목·T2 예수금·결제·T3 손익·성과·T4 주문·체결 신설, 증거금·담보·금현물=R10-B |
| CC-02 주문 | 현금 · 신용 · 금현물 ‖ 매수 · 매도 · 정정 · 취소 | **7/7 완료** — R11-T1 현금 매도(kt10001)·T2 정정(kt10002)·T3 취소(kt10003)·T4 신용 매수(kt10006)·T5 금현물 매수(kt50000) 신설. 신용 매도·정정·취소, 금현물 매도·정정·취소는 동일 골격의 조합이라 R11-B 묶음이 커버 |
| CC-06 탐색 | 업종 · 종목찾기 · 관심 · 테마 · 시장·VI · 조건검색 | **6/6 완료** — R04-T1 업종·T2 관심·T3 테마·T4 시장·VI·T5 조건검색 신설 (스트립 활성 상태 보드) |

2026-09-01 2차: 위 표의 미커버 15장을 전부 신설해 6개 카드 스트립의 버튼 상태가 전수 커버됐다.
카드 페이지 T-보드 총 21장. 주문 T-보드의 요청/응답 노드 이름은 해당 주문 TR(kt10001·kt10002·kt10003·kt10006·kt50000)의 공식 필드로 개명했다.

2026-09-01 3차 — 증명 페이지 분리와 화면 페이지 버튼 상태:

- **증명 페이지 신설.** 카드 페이지의 검증 장치 11장(R02-X1~X3 공식 원문 · R01/R02/R10/R11/R06-B 전체 항목 상세 · DV-1~3)을 새 `증명` 페이지로 이동했다. 실앱 렌더 대상이 아닌 보드는 이제 카드 페이지에 없다. `목록 펼침 검증` 2장은 인터랙션 정본에 가깝다는 기존 판정대로 카드 페이지에 남겼다.
- **화면 페이지 버튼 감사 (30보드).** 클릭 결과가 페이지 안에서 이어지는 짝: 계정 메뉴(12)→설정(13)·계좌 전환(21)·토큰 상태(19·20), 설정 nav 화면=13·계좌=14·모델=18, 계좌 인증 15→16→17, 주문 22→23, 모드 전환 25, 빈 상태 26, 복원 실패 30. 모드 버튼의 목적지는 각 모드 페이지(그래프·에이전트·플러그인·백테스트)가 소유한다.
- **미커버 4장 신설**: `31 · 설정 — 플러그인`(스니펫 등록·감사 로그), `32 · 설정 — 성향·이력`, `33 · 온보딩 — CLI 연결 실패`, `34 · 사이드바 검색 — 결과·빈 결과`. 이로써 감사에서 확인된 화면 페이지 버튼 미커버는 0이다.
- 신설 4장(31·32·33·34)은 `open_file(pageId)`로 화면 페이지를 활성화한 뒤 스크린샷 검수를 통과했다(2026-09-01). 증명 페이지 이동 11장도 이동 후 렌더 정상(R10-B 표본 확인). MCP `open_file`에 `pageId`를 주면 활성 페이지가 전환되므로, "마운트 안 된 아트보드 스크린샷 불가" 문제는 이 방법으로 우회할 수 있다.

R05·R06·R09와 A04 등 정본·질문형 보드에는 스트립이 없다(질문형 헤더). 좁은 데스크톱 C01~C07은 레이아웃 변형으로 보고 탭 상태 전수 대상에서 제외했다.

### 2026-09-01 4차 — 순위 모드 신설 (랭킹 TR 회수, B안)

카드 페이지 × 키움 REST API 전수 검수에서 확인된 랭킹·목록형 TR 공백(CC-03 17건 · CC-05 8건)을 **원장 귀속 이동 없이** 소유 카드 안의 `순위` 모드로 회수했다. A05(외국인 기간별 매매 상위)가 이미 CC-05 소유의 랭킹 보드라는 선례를 복제한 것으로, `canvas_card_registry`의 mode 자유도 안에서 해결하며 capability 배정·operation 수(76/12/88/31/50/42)·라우팅 계약은 불변이다.

| 신설 보드 | 활성 축 | 축 카탈로그(커버 TR) |
| --- | --- | --- |
| CC-03 / R01-T4 · 순위 — 주식 신호·순위 | 신고저가 ka10016 | ka10016 · 10017 · 10018 · 10019 · 10024 · 10025 · 10026 · 10028 · 10054 · 10055 (10축) |
| CC-03 / R01-T5 · 순위 — ETF 전체시세 | 전체시세 ka40004 | ka40004 · ka40001(기간 수익률 정렬) |
| CC-03 / R01-T6 · 순위 — ELW 순위 | 잔량 순위 ka30010 | ka30010 · 30009 · 30011 · 30001 · 30002 · 30005 (6축) |
| CC-05 / R03-T6 · 수급 — 순위 | 프로그램 순매수 상위 50 ka90003 | ka90003 · 10033 · 10069 · 10036 · 10065 · 90009 · 10039 · 10062 · kt20016 (9축, kt20017은 행 선택 시 표시) |

계약 원칙을 보드 자체에 명문화했다: ① 우측 레일 `이 탭이 담는 조회` 카탈로그가 축별 TR id를 명기(검수 지적 "조합 커버에 명문 근거 없음" 해소), ② 헤더 배지·푸터가 `조회 스냅샷 · 실시간 스트림 아님`을 선언, ③ `이어보기` 블록이 "행 선택 → 같은 카드 인스턴스의 상세 섹션 갱신"(한 호출=한 카드) 동선을 선언, ④ ka10054 VI의 탐색 카드 시장·VI 탭과의 문맥 공유를 배치 각주로 남김. 기존 스트립 보유 보드 9장(R01·R01-T1~T3 · R03-T1~T5)에는 `순위` 버튼을 비활성으로 추가해 스트립 구성을 일치시켰다.

**같은 날 2단계 — 잔여 공백 보완과 실앱 순위 모드 코어.**

Paper 쪽(전수 검수 잔여 공백):

- **CC-02 필드 완결** — 신용거래구분·대출일(R11-T4), 국내거래소구분(T2·T3·T5), 조건단가/정정조건단가(R11·T1·T2) 행 추가. T3의 모주문번호/원주문번호 중복 제거(원주문번호로 통일), 트레일 오배치 3건 정정(T3 `전량 취소 · 원주문` · T4 `신용융자` · T5 `10g · 금현물`), R11 메인에 "신용·금현물 매도·정정·취소는 3상품×4동작 스트립 조합으로 커버" 각주 명문화. 요청·응답 행 간격을 12→8px로 조여 우측 진행 상태 블록의 푸터 겹침(기존 결함 포함)을 해소.
- **CC-06 R04 정렬축 확장** — 조건 스트립을 `정렬 8칩(거래대금 활성·당일/전일 거래량·등락률·예상체결 등락률·호가잔량·잔량 급증·잔량율 급증) + 필터 + 세션(정규장·시간외 단일가)` 2줄로 재구성, 우측에 `정렬축 카탈로그 — 종목찾기 10축`(ka10020~10032·10098·10023, TR id 명기) 신설. discovery 랭킹 축 전체가 진입 자리를 얻음.
- **CC-05 시계열** — T3 우측 중복 프로그램 블록 → `프로그램 일자별 추이`(ka90010·ka90013·ka90007), T4 우측 중복 신용 블록 → `시장 전체 대차 체결·상환`(ka10068 시장축·ka90012)으로 교체.
- **CC-01 부채 축** — R10-T2 우측에 `부채·특수예수금·외화` 블록(kt00001 융자·특수예수금·외화 그룹, kt00016·kt00018 각주) 신설. "값이 0이어도 숨기지 않는다"를 각주로 고정.
- **R10-B 완결(증명 페이지)** — `증거금 구간별 주문가능 재원`(kt00011 6구간 · kt00012 3구간 · kt00013 9그룹 요약)과 `금현물 예수금·주문·미체결`(kt50021·50030·50031·50075) 블록을 추가해 위임처 공백을 채움.
- **CC-03 주기 축** — R01·R01-T3 주기 셀렉터에 틱·분·년 추가(ka10079·ka10080·ka10094, 금현물 ka50079·80·91·92 진입 축).

실앱 쪽(순위 모드 코어 — 원장·카운트·라우팅 계약 불변):

- `backend/athena_api/canvas_card_registry.py` — `OPERATION_PRESENTATION_OVERRIDES` 신설: 랭킹 28개 operation의 기본 mode/section을 `("ranking", "ranked-results")`로 파생(capability 소유권·카드 선택은 불변, plan 인자가 오면 여전히 우선). MCP 경로(`athena_mcp/canvas_data.py`)는 같은 resolve를 쓰므로 자동 전파.
- `backend/athena_api/api/canvas_push.py` — `_SECTION_TITLES_KO`(`ranked-results → 순위`)로 섹션 한글 제목 폴백 사다리를 보강.
- `app/lib/integrated-card-surface.js` — `panelKeyFor()`가 ranking 모드에서 operation_ref를 키에서 제외(1탭·다축 계약), `buttonLabel()`에 ranking→`순위` 단축 경로.
- 실시간은 무변경 — CC-03/CC-05 × ranking 정책이 없으므로 static, Paper의 "조회 스냅샷" 선언과 일치.
- 테스트: `test_canvas_card_registry.py`에 순위 기본값·plan 우선·비랭킹 불변 케이스, `integrated-card-surface.test.js`에 동일 panelKey·라벨 케이스 추가. 실행 결과 — 백엔드 표적 5파일 291 passed(유일한 실패 `test_view_identity_canonicalizes…`는 세션 이전부터 작업 트리에 있던 미커밋 `_view_instance_id` 수정이 원인, HEAD 대조로 확인), 광역 `-k canvas·selector·routing·recipe·semantic` 869 passed(실패 32건은 전부 `test_selector_autonomous_eval.py`의 환경 의존 실패로 stash 대조 결과 이번 변경과 무관), 앱 `integrated-card-surface`+`paper-card-routing`+`semantic-workspace` 84 passed·0 failed.
**같은 날 3단계 — 순위 모드 UI 완결(축 선택기·순위 표·라우팅 게이트·실렌더러 실측).**

- **축 선택기** — `app/lib/ranking-axis.js` 신설(UMD, column-fold 패턴). 카드별 축 카탈로그(CC-03: 주식10·ETF2·ELW6 / CC-05: 수급9 = 27축)와 `renderAxisStrip()`. 축 전환은 카드가 조회를 발명하지 않고 `AthenaShell.seedChatInput` 버스로 질문을 심는다(에이전트 "채팅에서 고치기"와 같은 경로). `ranking-axis.test.js`가 **백엔드 `OPERATION_PRESENTATION_OVERRIDES` 원문을 읽어 27축 1:1 동기화를 고정**한다.
- **패널 병합 확정** — `panelKeyFor()`의 ranking 키를 `mode:section`으로 좁혀 CC-05의 4개 capability(program-trading·credit-lending-short·investor-flow·broker)도 한 `순위` 탭을 공유한다. `canvas.js`의 `decorateRankingPanel()`이 패널 맨 위에 축 스트립을 세우고 `integrated-card-panel--ranking` 클래스를 스탬프.
- **순위 표 변형** — `integrated-cards.css`: 축 스트립 스타일 + 순위 패널의 표 내부 스크롤(max-height 420px)·sticky thead. 행별 상세는 기존 `<details>` 계약 재사용.
- **라우팅 게이트** — `test_question_to_card_routing.py`에 `expected_mode` 필드와 랭킹 3케이스(ka10019 가격급등락→CC-03 · ka30010 ELW 잔량→CC-03 · ka10069 대차 상위→CC-05, 전부 `expected_mode="ranking"`) 추가. 케이스 질문·인자는 셀렉터 실측으로 확정(ka10016 등은 typed 어휘 밖이라 케이스화하지 않음 — 후속 어휘 보강 과제).
- **실렌더러 실측** — `verify-integrated-cards.js`에 순위 시나리오 신설: 299 봉투 + 실시간 검증 + 캡처 뒤에 ka10019를 `ranking/ranked-results` 표현으로 재디스패치하고 `순위 탭 1개 · 축 칩 18개(CC-03) · 활성 base:ka10019 · 비활성 칩 클릭 → 채팅 시드 "거래량 갱신 종목 보여줘"`를 DOM으로 단언. 시나리오를 실시간 스냅샷 단계 **뒤**에 두는 이유: 순위 봉투는 정책 없는 정적 모드라 CC-03 리스를 내리므로 앞에 두면 스냅샷 설치가 "lease is not mounted"로 죽는다(실측).
- 검증(2026-09-01 재실행): `npm test -- --test-concurrency=1` **1,951 passed · 0 failed**, 백엔드 표적 6파일 **313 passed**, `npm run verify:integrated-cards` 완주(299 op · 3,705 fields · ranking 리포트 수록).

## 키우미 페이지 — 9/9

2026-09-01 전수검사에서 키우미를 별도 페이지(Paper `키우미`)로 대조했다. 오브 창과 입력
스트립 키우미는 같은 마스코트의 두 표면이라 한 페이지에서 함께 본다.

| Paper artboard | 실앱 소유 표면 | 판정 |
| --- | --- | --- |
| 01 · 키우미 — 상황별 표현·크기 매핑 | `orb.js` 표정 배선, `orb-window.js` 76px·360×400(최대 640) | 적용 |
| 02 · 키우미 — 표정 10종 | `orb.css` `[data-face]` 10개 눈 모양 | 적용 |
| 03 · 키우미 — 시선·시간 루프 | 신호 상태 14가지 · 시선 6축 · 깜빡임/두리번/숨 | 적용 |
| 04 · 키우미 — 대화·콘텐츠 전개 | 오브 대화 모드 8상태, 높이 상한 640 | 적용 |
| 05 · 키우미 — 셸 숨김·표시 | `athena:shell-visibility` 게이트 2상태 | 적용 |
| 06 · 키우미 메뉴 — 두 진입점과 항목 | `chat.js` `renderKiumiMenu()` 3섹션 8항목 | 적용 |
| 07 · 키우미 메뉴 — 셸 오버레이 | `#kiumiMenu` 380px 굴절형 오버레이 | 적용 |
| 08 · 키우미 — 입력 스트립 모드별 얼굴 | `shell.html` 얼굴 SVG 5종 + `chat.css` `[data-mode]` | 적용 |
| 09 · 키우미 — 미니 카드 10종 | `orb.js` 미니 카드 렌더러 + `lib/orb-mini-card.js` 상한 | 적용 |

### 미니 카드 계약 — 10/10

캔버스 렌더러가 받는 `canvas_type` 9종을 오브 미니 카드 10종이 전부 받는다. 이전에는
`table` · `chart` · 주문 티켓 셋만 카드가 되고 나머지는 카드 없이 텍스트로 흘러갔다.

| 미니 카드 | canvas_type | 상한 | 판정 |
| --- | --- | --- | --- |
| 01 표 | `table` | 헤더 제외 3행 | 적용 |
| 02 차트 | `chart` | 가격·등락·선 1개·날짜 2개 | 적용 |
| 03 사실 | `facts` | 값 있는 필드 5행 | 적용 |
| 04 복합 | `compound` | 스칼라 3개 + 표 2행 | 적용 |
| 05 미니 주문 티켓 | `facts` (`card_title=주문 티켓`) | 실행 버튼을 가진 유일한 카드 | 적용 |
| 06 주문 확인 | `action` | 허용 목록 2필드, 버튼 없음 | 적용 |
| 07 실시간 이벤트 | `event` | 수신 상태 배지 + 2건 | 적용 |
| 08 인증 상태 | `status` | 세 행 고정, 토큰 비노출 | 적용 |
| 09 본문 | `reader` | 첫 문단 140자 | 적용 |
| 10 스트림 | `stream` | 2건, 제목 + 시각·출처 | 적용 |

공통 규칙 5가지(값을 짓지 않는다 · 접었으면 밝힌다 · 실행은 05 하나 · 축소판이 아니다 ·
innerHTML 0건)는 `lib/orb-mini-card.test.js`가 순수 함수로, `probe-orb-mini-cards.js`가
실제 렌더러 DOM으로 각각 고정한다.

## 그래프 페이지 — 7/7

Paper 파일의 별도 **그래프** 페이지. 화면 페이지의 06·07·14·15·22가 이 페이지로 정리됐고,
06·07 두 보드는 2026-09-01 전수 검증에서 새로 추가했다(실앱에 있는데 Paper에 없던 것).

| Paper artboard | 실앱 소유 표면 | 판정 |
| --- | --- | --- |
| 01 · 셸 — 그래프 모드 · 요약 뷰 | `graph-mode/summary-table.js` · `theme-clusters.js` · `hidden-links.js` | 적용 |
| 02 · 셸 — 그래프 요약 · 행 선택 | `graph-mode/controller.js` 공통 패널(티어 대조) | 적용 |
| 03 · 그래프 — 기본 군집 지도 | `graph-mode/render.js` `renderClusterBubbles()` | 적용 |
| 04 · 그래프 — 군집 펼침 · 노드 선택 | `render.js` `renderClusterMap()` + 공통 패널 | 적용 |
| 05 · 그래프 — 수집·노출·브레인 제어 | `graph-mode/collection-settings.js` (그래프 모드 3번째 탭) | 적용 |
| 06 · 그래프 — 헤더 필터 (기간·정렬·연결 수) | `graph-mode/graph-filters.js` + `graph-mode-prefs.js` | **Paper 후행 추가** |
| 07 · 그래프 — 정직성 상태 (이름·인코딩·빈 값) | 이름 폴백 사다리 · 채움 인코딩 · 미분류 · 브레인 미준비 | **Paper 후행 추가** |

2026-09-01 전수 검증에서 정정한 실앱 쪽 괴리(전부 Paper 기준으로 맞춤):

| 항목 | 옛 상태 | 지금 |
| --- | --- | --- |
| 군집 버블 | 무지개 hue 회전 · 중앙 숫자 없음 · 채움 알파 = 응집도(0.74 → 74%) | Paper 실측 회귀: 반지름 `1.4+5.52√n`, 알파 `0.02+0.19·응집도`, 중앙 구성원 수, 흰 라벨 칩 |
| 2단계 | 펼친 군집 하나만 · 1단계 좌표 재사용(이름표 겹침) | 펼친 군집 + 이웃 1홉, 보이는 부분만 재배치(초점 중앙), 이웃 군집 타원 |
| 공통 패널 | 요약 카드 **안**에 있어 지도에서는 화면에 안 나타남 | 세 표면의 형제 — 요약·지도 어느 쪽에서 골라도 옆에 선다 |
| 패널 내용 | 보강 수 한 줄 + 숨은 연관만 | 헤더 부제(군집·연결·유일 노드 / 보강·최근), 관계 목록 전체, 왜 숨은 연관인가, CTA 리드인 |
| 필터 칩 | 정적 `<span>` — "최근 90일"이라 쓰고 아무것도 안 걸었다 | 기간·정렬·연결 수 실적용 + 기본값 아님을 파란 칩으로 표시 |
| 수집·노출 | 설정 오버레이에만 존재 | 그래프 모드 3번째 탭(보드 05) + 브레인 상태 카드 |
| 지도 노드 | 투자자 프로필이 차수 39 허브로 지도를 지배 | 분석용 투영에서 제외 — 그 관계는 노드 속성으로 패널에 |
| 성향 신호 표 | 대상 열에 64자 해시, 관계 배지 `prefers` 원문 | 종류 한글 라벨, 관계 한글 배지, "전체 N개" |
| 히어로 % | 상위 5 표본 — 실측에서 "사실 0% · 추론 100%" | 창 전체 `confidence_counts` |

## 검증 증거

- 앱 단위/회귀(2026-09-01 재실행): `npm test -- --test-concurrency=1` — 1,713 tests · 1,712 passed. 유일한 실패 `production 0B FID 10 binding updates the production snapshot observation`(`lib/semantic-workspace.test.js`)은 이번 변경 이전부터 실패하던 건으로, 변경 전 트리에서도 같은 결과다.
- 실 Electron 렌더러 + fixture 데이터: `npm run verify` — 전 단언 통과(2026-09-01 재실행, exit 0). 라이브 Kiwoom·브로커 검증은 포함하지 않는다. 플러그인 런타임은 아래 `verify:plugins`가 따로 실제 왕복으로 검증한다.
- 부팅: 일반·모션 감소·지연 온보딩 IPC 모두 `pass: true`.
- 반응형: 900px 2열/하단 작성창, 500px 44px 레일/프로젝트 오버레이 실측.
- 플러그인 런타임(2026-09-03): `npm run verify:plugins` — 단언 154건 전부 통과(`failures: 0`). 카탈로그 5종을 각각 등록 → 미승인 확인 → 승인 → probe → 도구 허용/해제 → 승인 철회 → 재승인 → 삭제까지 실제로 왕복했고, 그 위에 **5동작 사슬**을 승인 경로로 다시 잰다: 설치 제안→승인→설치됨 · 기능 허용/회수 · 켜기끄기 · 직접 등록 · 삭제, 그리고 거부 시 `mcp_servers.json`·`consent.json` 바이트 불변 · 같은 봉투 재승인 거절 · 설치 실패 시 미승인 행 잔존 0 · 차단 별칭 봉투는 실행기를 한 번도 부르지 않음. 외부 `npx`/`uvx` 다운로드가 일어나므로 네트워크가 필요하다. 리포트: `app/captures/VERIFY-PLUGINS-REPORT.json`.
- 플러그인 화면: `npm run verify`의 검증 3c — 허브 구역·빈 상태 문구·채팅 헤더 문구·설치 시트 7항목·GUI [설치]가 승인 카드로 합류(채팅 턴 0건)·승인/거부 버튼 32px·거부 시 카드 제거·출처 라벨 2종·`제안 대기`/`만료됨`/`거부됨` 3상태·기본 빌드 결과 턴이 `다음 실행부터 반영됩니다`라고 말하는지·관리 집계·직접 등록/감사 로그·기능 허용 배지 4종·probe 실패 배너를 실측. 레지스트리는 `ATHENA_MCP_REGISTRY_PATH`로 검증 프로필에 격리해 이 머신의 설치 목록과 무관하게 같은 결과가 나온다.
- 그래프 헤더 회귀: `(cd app && npx electron verify-graph-mode.js)` — 129건 전부 통과(`failures: 0`). `#chatModeHead`가 모드별 공용 헤더가 된 뒤에도 그래프 문구·가시성 3종이 그대로다.
- Paper 49/54: Kiumi 380px/8 SVG 액션, 프로젝트 설명·32px 액션·폴링 후 메뉴 지속 실측.
- 키우미 전수검사(2026-09-01): `lib/kiumi-face.test.js` 6건, `lib/orb-mini-card.test.js` 14건, `probe-orb-mini-cards.js` 22건(실 Electron 렌더러 DOM), `npm run verify:kiumi` 18건(살아 있는 셸·오브 창 실측 — 5모드 얼굴 배타·메뉴 380px/8항목·셸 숨김 게이트·궤도 링/배지 0건 규칙) 전부 통과.
- `probe-orb-mini-chart-card.js`는 15/15로 복구했다. 이전 9/15는 앱 결함이 아니라 프로브 결함이었다 — 상주 프로바이더 세션(`claude-chat-session.js:117`)이 세션 생성 시점의 실행 파일 경로를 붙들고 있어 턴 사이에 `ATHENA_CLAUDE_BIN`을 바꾸는 옛 기법이 무효가 됐다. 두 봉투를 한 턴에 보내도록 고쳤다.

- 그래프 모드 단위: `node --test app/lib/graph-mode/*.test.js` — 325 passed, 0 failed.
- 그래프 모드 실앱 전수(2026-09-01 신설): `electron app/verify-graph-mode.js` — 75 checks, 0 failures.
  실제 백엔드 + 실제 성향 그래프(장기·ETF·중대형주 페르소나 41 엔티티 / 91 관계 /
  성향 신호 48건)를 프로덕션 셸에 붙여 보드 01~05의 요소·클릭·필터를 전부 잰다.
  씨앗은 `backend/scripts/seed_long_term_etf_persona.py`(결정적). 캡처는
  `artifacts/graph-mode/*.png`, 리포트는 `artifacts/graph-mode/verify-graph-mode.json`.
- 백엔드 브레인/그래프: `pytest -k "brain or graph or projection"` — 443 passed.
- 백엔드 카드 변환: `backend/tests/unit/test_canvas_transform.py` — 174 passed.
- **미해결(그래프와 무관, 선행 상태)**: `npm run verify`(앱 전역 하네스)는 부팅 shot 02
  직후 렌더러 `executeJavaScript`에서 unhandledRejection이 나며 캡처 10장에서 멈춘다.
  2026-09-01 확인: 이 작업 변경을 전부 stash한 깨끗한 트리에서도 같은 지점에서 같은
  방식으로 멈춘다 — 그래프 변경이 만든 회귀가 아니다. 그래프 표면의 실앱 검증은 위
  `verify-graph-mode.js`가 독립적으로 덮는다.
- 화면 정의/주입/카드 사실: 21 passed.
- 렌더 캡처와 JSON 리포트: `app/captures/` 및 `app/captures/VERIFY-REPORT.json`.

## 의도적으로 만들지 않은 중복 화면

- Paper 37–39는 이름 그대로 이전 셸 참고본이므로 현재 셸과 별도 라우트를 만들지 않았다.
- 카드 페이지의 31개 보드는 앱 네비게이션 화면이 아니라, 299개 작업 응답이 어떤 카드 창으로 보이는지 정하는 렌더 계약이다.
- OAuth 인증 2종은 299 카드 집계에서 제외하고 화면 27–29의 인증/계좌 상태에 남겼다.

---

## 에이전트 페이지 전수 대조 — 2026-09-01

Paper 파일이 페이지 단위로 재편되면서(`화면 · 그래프 · 카드 · 에이전트 · 플러그인 · 키우미 · 백테스트`)
에이전트 설계는 더 이상 `화면` 페이지의 39–43번이 아니라 **`에이전트` 페이지**에 산다.
이 절은 그 페이지와 실앱 에이전트 모드를 요소 단위로 대조한 결과다.

기준 시각: 2026-09-01 KST<br>
Paper 페이지: `에이전트` — **6 artboards**(01 셸·알림 파생 방, 02 알람 센터·라이브 관제, 03 실행 이력·결과, 04 프로액티브, 05 새 작업은 채팅에서, 06 작업 설정)<br>
실앱 소유 표면: `app/lib/agent-canvas.js` · `app/lib/agent-sidebar-list.js` · `app/canvas.js`(배선) · `app/shell.css`

### 판정 요약

| Paper artboard | 실앱 소유 표면 | 판정 |
| --- | --- | --- |
| 01 · 셸 — 알림 파생 방 | `shell.html #roomHeadBanner`, `lib/sidebar.js` 알림 방 | 적용 |
| 02 · 에이전트 — 알람 센터 · 라이브 관제 | `agent-canvas.js` 알람/라이브 뷰 | 적용 |
| 03 · 에이전트 — 실행 이력·결과 | `agent-canvas.js` 드릴인(브레드크럼·이력) | 적용 |
| 04 · 에이전트 — 프로액티브 | `agent-canvas.js` 제안 뷰 | 적용 |
| 05 · 에이전트 — 새 작업은 채팅에서 | `agent-canvas.js` 작업 뷰 | 적용 |
| 06 · 에이전트 — 작업 설정 (보기 전용) | `agent-canvas.js` 드릴인 설정 탭 | 적용 (이번에 신설) |

### Paper에 있고 앱에 없던 것 — 앱에 구현

| # | Paper 근거 | 앱 구현 |
| --- | --- | --- |
| 1 | 보드 05 하단 `동선 규칙` ①②③ | `agent-route-rules` 패널. 세 줄은 Paper 원문 그대로이며, 화면 자신의 계약이라 `data-source`를 붙이지 않는다 |
| 2 | 보드 03 우상단 `[이력][설정]` 세그먼트 | `agent-history-tabs` + `setHistoryTab()`. 드릴인에서만 보이고, 닫으면 항상 `이력`으로 초기화된다 |
| 3 | 보드 03 `최근 30회`의 날짜 그룹 머리 | `runDateGroupLabel()` — `오늘 — M/D 요일` · `어제 — …` · `M/D 요일`. 행 시각은 `HH:MM`만 남는다 |
| 4 | 보드 03 `지난 실행 24건 더` | `agent-list-more` 푸터. 죽은 글자가 아니라 나머지를 펼치는 버튼이다(접힘 6행) |
| 5 | 보드 02 `지난 알람 24건 더` | 같은 `agent-list-more` 규칙을 알람 피드에 적용(접힘 6행) |
| 6 | 보드 02 `다음 24시간` 행의 시각 열·갈래별 점 색 | `agent-live-timeline-time` + 갈래별 `colorVar`(예약=ok · 프로액티브=brand · 감시=info) |
| 7 | 보드 04 스트립 우측 `신호 312 · 12분 전` | `freshestSignalAge()` — profile-summary의 실제 `observed_at` 최신값에서 계산. 값이 없으면 신선도를 지어내지 않고 `신호 N`만 남긴다 |
| 8 | 보드 04 제안 카드의 설명 + `근거:` 2줄 | `agent-proactive-card-desc` + `agent-proactive-card-rationale` 분리 |
| 9 | 보드 02 알람 행의 갈래 아이콘 2종 | `alarmIcon()` — routine-fired 이벤트의 `mode`로 `● 예약 실행` / `◆ 조건 감시`를 가른다. `mode`는 `sidebar.js`가 `AthenaNotify.list()`로 실어 보낸다 |

### 앱에 있고 Paper에 없던 것 — Paper에 추가

보드 05가 앱의 "작업" 뷰 실제 구성과 어긋나 있었다(리스트 + 동선 규칙뿐이었다). 다음을 Paper에 그렸다.

- 뷰 탭 4종 통일 — `작업 · 알람 N · 라이브 · 제안 N`. 이전에는 보드 02가 `라이브`만, 보드 04·05가 `제안`만 갖고 있었다
- 작업 뷰 머리 — 부제(`루틴 N · 감시 M 진행 중`) · 리스트 필터 탭(`모두 · 활성 · 일시중지`) · `작업 검색` · `＋ 새 작업 · 채팅에서`
- 통계 카드 4장 — `다음 실행`(live) · `진행 중`(fixture) · `오늘 발화`(live) · `성향 제안`(live · accent)
- 본문 2열 — 리스트 열 330px + 상세 열(배지·제목·일시중지·설명·세부 정보·최근 실행·`전체 이력 보기 →`·`채팅에서 열기 ↗`)
- 리스트 열 하단 `제안 — 그래프 성향 기반` 미니 목록과 `추가` 칩
- 사이드바 모드 5종 — 보드 02–06에 `플러그인`·`백테스트`를 더해 실앱 `sidebar-mode-nav`와 맞췄다

### 데이터 계약과 어긋나 Paper를 정정한 것

Paper 목업이 백엔드가 만들 수 없는 상태를 그리고 있던 두 곳은 **앱을 목업에 맞추지 않고 Paper를 실계약에 맞췄다**.
지어낸 데이터를 화면에 올리지 않는다는 기존 원칙(P3)과, 죽은 아이콘을 남기지 않는다는 원칙 둘 다를 지키기 위해서다.

| 위치 | 목업 | 실계약 | 정정 |
| --- | --- | --- | --- |
| 보드 03 실행 행 아이콘 | `● ⚠ ↻ ◆` 4종 | `ledger.py`의 verdict는 `fired · near · suppressed` 3종뿐 | `● 발화 / ◐ 근접 / ○ 억제`로 교체. `대체 실행`·`재시도` 행은 `근접 — 임계 미달` · `억제 — 쿨다운 중`으로 다시 씀 |
| 보드 02 알람 행 아이콘 | `◆ ● ⚠ ❚❚` 4종 | `handleRoutineEvent`가 `routine-fired` 외에는 방을 만들지 않는다 | `mode`로 가르는 `◆ · ●` 2종만 남기고, 대응 이벤트가 없는 `⚠ 응답 지연` · `❚❚ 일시중지` 행은 실제 발화 행으로 교체 |

### 새로 설계하고 구현한 기능 — 드릴인 `설정` 탭

보드 03이 `[이력][설정]` 세그먼트를 그렸지만 `설정`의 내용은 어디에도 없었다.
Paper에 **보드 06 · 작업 설정 (보기 전용)**을 신설해 먼저 정의하고, 같은 계약으로 앱에 구현했다.

- 내용은 백엔드가 실제로 주는 필드만이다 — `조건 · 모드 · 소스 · 종목 · 쿨다운 · 브리핑 모델 · 다음 실행 · 만료 · 생성`
- `브리핑 모델`·`다음 실행`은 `mode`가 아니라 **그 값이 실제로 있을 때** 붙는다(예약 드릴인이 열리지 않는 지금도 죽은 가지가 안 생긴다)
- 값을 바꾸는 입력은 두지 않는다. 고치는 경로는 `채팅에서 고치기 ↗` 하나뿐이며, 이는 동선 규칙 ②("편집도 채팅으로 — 상세 패널은 보기 전용")를 화면으로 지킨 것이다

### 접근성

보드 02–05에서 11–12px 의미 텍스트에 남아 있던 저대비 `#8B93A1` **72개**를 실앱 토큰과 같은 `#626B76`(`--color-k-faint`)로 통일했다.
동선 규칙 3줄은 본문 성격이라 `#5B6270`(`--color-k-dim`)을 쓴다. 검수 대장의 "작은 본문은 최소 `#5B6270` 수준" 공통 기준과 같은 방향이다.

### 검증 증거 — 2026-09-01

| 게이트 | 결과 |
| --- | --- |
| `npm test`(앱 단위·회귀) | **1,722 passed · 0 failed · 0 skipped** |
| `npm run verify:agent-paper-parity`(실 Electron 렌더러) | **34/34 단언 통과 · 렌더러 콘솔 에러 0** |
| `uv run pytest`(백엔드 전체) | **2,491 passed · 0 failed · 5 skipped** |

새로 추가한 `app/probe-agent-paper-parity.js`는 위 대조표의 계약을 실제 렌더러에서 재는 좁은 프로브다.
뷰 탭 4종, 동선 규칙 3줄 원문과 `data-source` 부재, 타임라인 시각 열·점 색, 알람 갈래 아이콘 2종,
접기 푸터가 실제로 펼쳐지는지, 드릴인 세그먼트·날짜 그룹 병합·보기 전용 설정 패널과
`채팅에서 고치기 ↗`가 시트 대신 채팅 입력에 문장을 심는지를 모두 클릭으로 확인한다.

**`npm run verify` 전체 하네스는 이번에 완주하지 못했다.** 검증3b(반응형·Snap)의
`responsiveSettle`이 `requestAnimationFrame` 두 프레임을 기다리는데, 창이 가려지거나 스로틀되는
세션에서는 그 대기가 끝나지 않는다. **이 저장소를 이번 변경 이전 상태(HEAD)로 되돌려 같은 명령을
돌려도 정확히 같은 지점에서 멈추는 것을 확인**했으므로 이번 에이전트 변경과 무관한 기존 환경 의존
행업이다. 그래서 실렌더러 증거는 위의 전용 프로브로 확보했고, 전체 하네스의 rAF 대기에
타임아웃을 두는 것은 별도 과제로 남긴다.

## 에이전트 페이지 — 코드 알람 보드 09~12 대조 (2026-09-04)

Paper `에이전트 A-2` 리뷰 보드 09~12가 코드 알람(감시 함수) 갈래를 새로 그린다.
09·10은 채팅 쪽(승인·검사 카드, Step 6), 11·12는 캔버스 쪽(노드 카드·상세, Step 7)이다.
실앱 소유 표면: `app/lib/agent-canvas.js` · `app/lib/watch-nodes.js`(문구 계산) ·
`app/lib/watch-check-card.js`(채팅 문구) · `app/canvas.js`(배선)

### 판정 요약

| Paper artboard | 실앱 소유 표면 | 판정 |
| --- | --- | --- |
| 09 · 새 알람 — AI가 감시 함수를 만드는 중 | `chat.js` 승인·검사 카드(Step 6) + 캔버스 초안 상세의 노드 자리·접힌 코드 | 적용 (자동 검사 5줄 진행 로그는 채팅 몫) |
| 10 · 검사 결과 · 승인 | `chat.js` 검사 결과 카드 + `agent-canvas.js` 초안 상세(검사 요약·「검사」·노드 4칸) | 적용 |
| 11 · 한 바퀴 고침 — 「방금 바뀜」·칸 칩 | `agent-canvas.js` 노드 카드(`agent-node-*`) | 적용 (「되돌리기」·「지난 고침 N건」은 채팅 쪽 이력, 캔버스에 중복 안 만듦) |
| 12 · 활성 코드 알람 상세 | `agent-canvas.js` `renderCodeDetail()` | 적용 |

### 보드 → 화면 대응

| Paper 근거 | 앱 구현 |
| --- | --- |
| 보드 12 목록 행 `◆ 코드 감시 · 장중 1분마다` | `statusRowIcon()`의 `code-watch` 분기(◆, 일시중지는 흐리게) + `toWatchItem()`의 `WatchNodes.watchSubLabel()`. 「주기 확인」 폴백으로 절대 안 떨어진다 |
| 보드 12 머리 `활성` · 제목 · `코드 감시 · v2` | `agent-status-badge` + `agent-detail-title` + `agent-code-kind`(해시 앞 6글자). 해시가 없으면 갈래 이름만 남기고 버전을 지어내지 않는다 |
| 보드 12 제어 행 `일시중지` `취소` `고치기 — 말로` | `agent-code-controls` — 앞 둘은 기존 `athena:routine-pause/resume`·`athena:routine-cancel`, 셋째는 채팅으로 넘기는 한 경로 |
| 보드 10·11 노드 카드 4칸 | `agent-node-card` — 한국어 제목 / 영어 함수명 작게 / 「들어감」 행들 / 구분선 / 「나옴」 굵게(R7 순서 그대로). 카드 수 = 감시 함수의 최상위 함수 수 |
| 보드 11 `방금 바뀜` 배지 | `agent-node-badge` — 백엔드가 `changed`로 표시한 칸에만 |
| 보드 11 선택 칸의 진한 테두리 + `이상해요`·`물어볼게요` | `agent-node-card.is-selected` + `agent-node-chip` 2종. 칩은 그 칸을 지목한 문장을 채팅 입력에 심는다(시트 없음 — 동선 규칙②) |
| 보드 10 `코드 · 참고 · 펼치기` | `agent-code-source-toggle`. v1은 원문을 가져오지 않고 경로만 편다(코드는 참고, P2) |
| 보드 12 `울린 기록 · 최근` + `전체 이력 보기 →` | `agent-code-fires` — 드릴인(10단계)과 같은 `/{id}/runs` 원천이다(두 개의 진실 금지) |
| 보드 12 `설정 확인 주기 장중 1분 쿨다운 1일 만료 2026-10-03` | `agent-detail-fields` 3행 + 「말로 바꾸는 건 쿨다운 · 만료 · 설명」 안내. **조건 편집 폼은 없다** — 조건은 감시 함수 자체라 「고치기 — 말로」로만 바뀐다 |
| 보드 10 `검사 결과 · 지난 30일 4번 · 마지막 8/26` | 초안 상세의 `agent-code-check-summary` + 「검사」 버튼(`athena:routine-watch-check`). 채팅 초안 카드의 검사 칩과 같은 본문·같은 통로다 |
| 개념 문서 P4 | 초안 상세의 `어제까지로 세었음 · 오늘은 진행 중` — 검사(D-1)와 실행(오늘 진행 중 봉)의 비대칭을 숨기지 않는다 |

### Paper와 다르게 한 것

- **켜진 알람의 「고치기 — 말로」는 바로 채팅으로 넘어가지 않는다.** 활성 상태에서는 먼저 멈춤을
  묻는 줄(`일시중지하고 고치기` · `그대로 두기`)이 상세 안에 뜬다. 켜진 알람의 코드 파일은 백엔드가
  덮어쓰기를 막기 때문인데, 그 거절 사유(코드 번호)는 화면에 옮기지 않고 **사람이 할 수 있는 다음
  행동 둘만** 보여준다(계획 R10·A-12). 일시중지·초안에서는 확인 없이 바로 넘어간다.
- **보드 11의 「되돌리기」·「지난 고침 1건」은 캔버스에 만들지 않았다.** 그 둘은 채팅 카드가 소유한
  고침 이력이다 — 같은 것을 두 화면이 각각 그리면 두 개의 진실이 생긴다.
- **보드 09의 자동 검사 진행 5줄(입력 확인 · 함수 만듦 · 검사 1/3…)도 채팅 몫이다.** 캔버스는
  결과가 비치는 곳이라는 동선 규칙③을 그대로 지킨다.

### 검증 증거 — 2026-09-04

| 게이트 | 결과 |
| --- | --- |
| `cd app && npm test` | **2,696 passed · 0 failed** (코드 알람 19건 추가) |
| `cd app && npm run verify:agent-paper-parity` | **58/58 단언 통과 · 렌더러 콘솔 에러 0** (기존 35 + 코드 알람 23) |
